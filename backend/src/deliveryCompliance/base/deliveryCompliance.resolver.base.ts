/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DeliveryCompliance } from "./DeliveryCompliance";
import { DeliveryComplianceCountArgs } from "./DeliveryComplianceCountArgs";
import { DeliveryComplianceFindManyArgs } from "./DeliveryComplianceFindManyArgs";
import { DeliveryComplianceFindUniqueArgs } from "./DeliveryComplianceFindUniqueArgs";
import { CreateDeliveryComplianceArgs } from "./CreateDeliveryComplianceArgs";
import { UpdateDeliveryComplianceArgs } from "./UpdateDeliveryComplianceArgs";
import { DeleteDeliveryComplianceArgs } from "./DeleteDeliveryComplianceArgs";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { User } from "../../user/base/User";
import { DeliveryComplianceService } from "../deliveryCompliance.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DeliveryCompliance)
export class DeliveryComplianceResolverBase {
  constructor(
    protected readonly service: DeliveryComplianceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DeliveryCompliance",
    action: "read",
    possession: "any",
  })
  async _deliveryCompliancesMeta(
    @graphql.Args() args: DeliveryComplianceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DeliveryCompliance])
  @nestAccessControl.UseRoles({
    resource: "DeliveryCompliance",
    action: "read",
    possession: "any",
  })
  async deliveryCompliances(
    @graphql.Args() args: DeliveryComplianceFindManyArgs
  ): Promise<DeliveryCompliance[]> {
    return this.service.deliveryCompliances(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DeliveryCompliance, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DeliveryCompliance",
    action: "read",
    possession: "own",
  })
  async deliveryCompliance(
    @graphql.Args() args: DeliveryComplianceFindUniqueArgs
  ): Promise<DeliveryCompliance | null> {
    const result = await this.service.deliveryCompliance(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryCompliance)
  @nestAccessControl.UseRoles({
    resource: "DeliveryCompliance",
    action: "create",
    possession: "any",
  })
  async createDeliveryCompliance(
    @graphql.Args() args: CreateDeliveryComplianceArgs
  ): Promise<DeliveryCompliance> {
    return await this.service.createDeliveryCompliance({
      ...args,
      data: {
        ...args.data,

        delivery: {
          connect: args.data.delivery,
        },

        verifiedBy: args.data.verifiedBy
          ? {
              connect: args.data.verifiedBy,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryCompliance)
  @nestAccessControl.UseRoles({
    resource: "DeliveryCompliance",
    action: "update",
    possession: "any",
  })
  async updateDeliveryCompliance(
    @graphql.Args() args: UpdateDeliveryComplianceArgs
  ): Promise<DeliveryCompliance | null> {
    try {
      return await this.service.updateDeliveryCompliance({
        ...args,
        data: {
          ...args.data,

          delivery: {
            connect: args.data.delivery,
          },

          verifiedBy: args.data.verifiedBy
            ? {
                connect: args.data.verifiedBy,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DeliveryCompliance)
  @nestAccessControl.UseRoles({
    resource: "DeliveryCompliance",
    action: "delete",
    possession: "any",
  })
  async deleteDeliveryCompliance(
    @graphql.Args() args: DeleteDeliveryComplianceArgs
  ): Promise<DeliveryCompliance | null> {
    try {
      return await this.service.deleteDeliveryCompliance(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "delivery",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getDelivery(
    @graphql.Parent() parent: DeliveryCompliance
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getDelivery(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "verifiedBy",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getVerifiedBy(
    @graphql.Parent() parent: DeliveryCompliance
  ): Promise<User | null> {
    const result = await this.service.getVerifiedBy(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
