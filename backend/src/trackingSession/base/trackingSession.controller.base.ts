/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TrackingSessionService } from "../trackingSession.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TrackingSessionCreateInput } from "./TrackingSessionCreateInput";
import { TrackingSession } from "./TrackingSession";
import { TrackingSessionFindManyArgs } from "./TrackingSessionFindManyArgs";
import { TrackingSessionWhereUniqueInput } from "./TrackingSessionWhereUniqueInput";
import { TrackingSessionUpdateInput } from "./TrackingSessionUpdateInput";
import { TrackingPointFindManyArgs } from "../../trackingPoint/base/TrackingPointFindManyArgs";
import { TrackingPoint } from "../../trackingPoint/base/TrackingPoint";
import { TrackingPointWhereUniqueInput } from "../../trackingPoint/base/TrackingPointWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TrackingSessionControllerBase {
  constructor(
    protected readonly service: TrackingSessionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TrackingSession })
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createTrackingSession(
    @common.Body() data: TrackingSessionCreateInput
  ): Promise<TrackingSession> {
    return await this.service.createTrackingSession({
      data: {
        ...data,

        delivery: {
          connect: data.delivery,
        },
      },
      select: {
        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        drivenMiles: true,
        id: true,
        startedAt: true,
        status: true,
        stoppedAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TrackingSession] })
  @ApiNestedQuery(TrackingSessionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async trackingSessions(
    @common.Req() request: Request
  ): Promise<TrackingSession[]> {
    const args = plainToClass(TrackingSessionFindManyArgs, request.query);
    return this.service.trackingSessions({
      ...args,
      select: {
        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        drivenMiles: true,
        id: true,
        startedAt: true,
        status: true,
        stoppedAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TrackingSession })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async trackingSession(
    @common.Param() params: TrackingSessionWhereUniqueInput
  ): Promise<TrackingSession | null> {
    const result = await this.service.trackingSession({
      where: params,
      select: {
        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        drivenMiles: true,
        id: true,
        startedAt: true,
        status: true,
        stoppedAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TrackingSession })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateTrackingSession(
    @common.Param() params: TrackingSessionWhereUniqueInput,
    @common.Body() data: TrackingSessionUpdateInput
  ): Promise<TrackingSession | null> {
    try {
      return await this.service.updateTrackingSession({
        where: params,
        data: {
          ...data,

          delivery: {
            connect: data.delivery,
          },
        },
        select: {
          createdAt: true,

          delivery: {
            select: {
              id: true,
            },
          },

          drivenMiles: true,
          id: true,
          startedAt: true,
          status: true,
          stoppedAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TrackingSession })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteTrackingSession(
    @common.Param() params: TrackingSessionWhereUniqueInput
  ): Promise<TrackingSession | null> {
    try {
      return await this.service.deleteTrackingSession({
        where: params,
        select: {
          createdAt: true,

          delivery: {
            select: {
              id: true,
            },
          },

          drivenMiles: true,
          id: true,
          startedAt: true,
          status: true,
          stoppedAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/points")
  @ApiNestedQuery(TrackingPointFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "read",
    possession: "any",
  })
  async findPoints(
    @common.Req() request: Request,
    @common.Param() params: TrackingSessionWhereUniqueInput
  ): Promise<TrackingPoint[]> {
    const query = plainToClass(TrackingPointFindManyArgs, request.query);
    const results = await this.service.findPoints(params.id, {
      ...query,
      select: {
        createdAt: true,
        id: true,
        lat: true,
        lng: true,
        recordedAt: true,

        session: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/points")
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "update",
    possession: "any",
  })
  async connectPoints(
    @common.Param() params: TrackingSessionWhereUniqueInput,
    @common.Body() body: TrackingPointWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      points: {
        connect: body,
      },
    };
    await this.service.updateTrackingSession({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/points")
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "update",
    possession: "any",
  })
  async updatePoints(
    @common.Param() params: TrackingSessionWhereUniqueInput,
    @common.Body() body: TrackingPointWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      points: {
        set: body,
      },
    };
    await this.service.updateTrackingSession({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/points")
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "update",
    possession: "any",
  })
  async disconnectPoints(
    @common.Param() params: TrackingSessionWhereUniqueInput,
    @common.Body() body: TrackingPointWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      points: {
        disconnect: body,
      },
    };
    await this.service.updateTrackingSession({
      where: params,
      data,
      select: { id: true },
    });
  }
}
