/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { TrackingSession } from "./TrackingSession";
import { TrackingSessionCountArgs } from "./TrackingSessionCountArgs";
import { TrackingSessionFindManyArgs } from "./TrackingSessionFindManyArgs";
import { TrackingSessionFindUniqueArgs } from "./TrackingSessionFindUniqueArgs";
import { CreateTrackingSessionArgs } from "./CreateTrackingSessionArgs";
import { UpdateTrackingSessionArgs } from "./UpdateTrackingSessionArgs";
import { DeleteTrackingSessionArgs } from "./DeleteTrackingSessionArgs";
import { TrackingPointFindManyArgs } from "../../trackingPoint/base/TrackingPointFindManyArgs";
import { TrackingPoint } from "../../trackingPoint/base/TrackingPoint";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { TrackingSessionService } from "../trackingSession.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => TrackingSession)
export class TrackingSessionResolverBase {
  constructor(
    protected readonly service: TrackingSessionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "read",
    possession: "any",
  })
  async _trackingSessionsMeta(
    @graphql.Args() args: TrackingSessionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [TrackingSession])
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "read",
    possession: "any",
  })
  async trackingSessions(
    @graphql.Args() args: TrackingSessionFindManyArgs
  ): Promise<TrackingSession[]> {
    return this.service.trackingSessions(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => TrackingSession, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "read",
    possession: "own",
  })
  async trackingSession(
    @graphql.Args() args: TrackingSessionFindUniqueArgs
  ): Promise<TrackingSession | null> {
    const result = await this.service.trackingSession(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => TrackingSession)
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "create",
    possession: "any",
  })
  async createTrackingSession(
    @graphql.Args() args: CreateTrackingSessionArgs
  ): Promise<TrackingSession> {
    return await this.service.createTrackingSession({
      ...args,
      data: {
        ...args.data,

        delivery: {
          connect: args.data.delivery,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => TrackingSession)
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "update",
    possession: "any",
  })
  async updateTrackingSession(
    @graphql.Args() args: UpdateTrackingSessionArgs
  ): Promise<TrackingSession | null> {
    try {
      return await this.service.updateTrackingSession({
        ...args,
        data: {
          ...args.data,

          delivery: {
            connect: args.data.delivery,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => TrackingSession)
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "delete",
    possession: "any",
  })
  async deleteTrackingSession(
    @graphql.Args() args: DeleteTrackingSessionArgs
  ): Promise<TrackingSession | null> {
    try {
      return await this.service.deleteTrackingSession(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [TrackingPoint], { name: "points" })
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "read",
    possession: "any",
  })
  async findPoints(
    @graphql.Parent() parent: TrackingSession,
    @graphql.Args() args: TrackingPointFindManyArgs
  ): Promise<TrackingPoint[]> {
    const results = await this.service.findPoints(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "delivery",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getDelivery(
    @graphql.Parent() parent: TrackingSession
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getDelivery(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
