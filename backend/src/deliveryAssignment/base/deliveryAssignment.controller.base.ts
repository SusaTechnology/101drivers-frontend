/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DeliveryAssignmentService } from "../deliveryAssignment.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeliveryAssignmentCreateInput } from "./DeliveryAssignmentCreateInput";
import { DeliveryAssignment } from "./DeliveryAssignment";
import { DeliveryAssignmentFindManyArgs } from "./DeliveryAssignmentFindManyArgs";
import { DeliveryAssignmentWhereUniqueInput } from "./DeliveryAssignmentWhereUniqueInput";
import { DeliveryAssignmentUpdateInput } from "./DeliveryAssignmentUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DeliveryAssignmentControllerBase {
  constructor(
    protected readonly service: DeliveryAssignmentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DeliveryAssignment })
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDeliveryAssignment(
    @common.Body() data: DeliveryAssignmentCreateInput
  ): Promise<DeliveryAssignment> {
    return await this.service.createDeliveryAssignment({
      data: {
        ...data,

        assignedBy: data.assignedBy
          ? {
              connect: data.assignedBy,
            }
          : undefined,

        delivery: {
          connect: data.delivery,
        },

        driver: {
          connect: data.driver,
        },
      },
      select: {
        assignedAt: true,

        assignedBy: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        id: true,
        reason: true,
        unassignedAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DeliveryAssignment] })
  @ApiNestedQuery(DeliveryAssignmentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deliveryAssignments(
    @common.Req() request: Request
  ): Promise<DeliveryAssignment[]> {
    const args = plainToClass(DeliveryAssignmentFindManyArgs, request.query);
    return this.service.deliveryAssignments({
      ...args,
      select: {
        assignedAt: true,

        assignedBy: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        id: true,
        reason: true,
        unassignedAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DeliveryAssignment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deliveryAssignment(
    @common.Param() params: DeliveryAssignmentWhereUniqueInput
  ): Promise<DeliveryAssignment | null> {
    const result = await this.service.deliveryAssignment({
      where: params,
      select: {
        assignedAt: true,

        assignedBy: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        id: true,
        reason: true,
        unassignedAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DeliveryAssignment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDeliveryAssignment(
    @common.Param() params: DeliveryAssignmentWhereUniqueInput,
    @common.Body() data: DeliveryAssignmentUpdateInput
  ): Promise<DeliveryAssignment | null> {
    try {
      return await this.service.updateDeliveryAssignment({
        where: params,
        data: {
          ...data,

          assignedBy: data.assignedBy
            ? {
                connect: data.assignedBy,
              }
            : undefined,

          delivery: {
            connect: data.delivery,
          },

          driver: {
            connect: data.driver,
          },
        },
        select: {
          assignedAt: true,

          assignedBy: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          delivery: {
            select: {
              id: true,
            },
          },

          driver: {
            select: {
              id: true,
            },
          },

          id: true,
          reason: true,
          unassignedAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DeliveryAssignment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDeliveryAssignment(
    @common.Param() params: DeliveryAssignmentWhereUniqueInput
  ): Promise<DeliveryAssignment | null> {
    try {
      return await this.service.deleteDeliveryAssignment({
        where: params,
        select: {
          assignedAt: true,

          assignedBy: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          delivery: {
            select: {
              id: true,
            },
          },

          driver: {
            select: {
              id: true,
            },
          },

          id: true,
          reason: true,
          unassignedAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
