/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DeliveryAssignment } from "./DeliveryAssignment";
import { DeliveryAssignmentCountArgs } from "./DeliveryAssignmentCountArgs";
import { DeliveryAssignmentFindManyArgs } from "./DeliveryAssignmentFindManyArgs";
import { DeliveryAssignmentFindUniqueArgs } from "./DeliveryAssignmentFindUniqueArgs";
import { CreateDeliveryAssignmentArgs } from "./CreateDeliveryAssignmentArgs";
import { UpdateDeliveryAssignmentArgs } from "./UpdateDeliveryAssignmentArgs";
import { DeleteDeliveryAssignmentArgs } from "./DeleteDeliveryAssignmentArgs";
import { User } from "../../user/base/User";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { Driver } from "../../driver/base/Driver";
import { DeliveryAssignmentService } from "../deliveryAssignment.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DeliveryAssignment)
export class DeliveryAssignmentResolverBase {
  constructor(
    protected readonly service: DeliveryAssignmentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "read",
    possession: "any",
  })
  async _deliveryAssignmentsMeta(
    @graphql.Args() args: DeliveryAssignmentCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DeliveryAssignment])
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "read",
    possession: "any",
  })
  async deliveryAssignments(
    @graphql.Args() args: DeliveryAssignmentFindManyArgs
  ): Promise<DeliveryAssignment[]> {
    return this.service.deliveryAssignments(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DeliveryAssignment, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "read",
    possession: "own",
  })
  async deliveryAssignment(
    @graphql.Args() args: DeliveryAssignmentFindUniqueArgs
  ): Promise<DeliveryAssignment | null> {
    const result = await this.service.deliveryAssignment(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryAssignment)
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "create",
    possession: "any",
  })
  async createDeliveryAssignment(
    @graphql.Args() args: CreateDeliveryAssignmentArgs
  ): Promise<DeliveryAssignment> {
    return await this.service.createDeliveryAssignment({
      ...args,
      data: {
        ...args.data,

        assignedBy: args.data.assignedBy
          ? {
              connect: args.data.assignedBy,
            }
          : undefined,

        delivery: {
          connect: args.data.delivery,
        },

        driver: {
          connect: args.data.driver,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryAssignment)
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "update",
    possession: "any",
  })
  async updateDeliveryAssignment(
    @graphql.Args() args: UpdateDeliveryAssignmentArgs
  ): Promise<DeliveryAssignment | null> {
    try {
      return await this.service.updateDeliveryAssignment({
        ...args,
        data: {
          ...args.data,

          assignedBy: args.data.assignedBy
            ? {
                connect: args.data.assignedBy,
              }
            : undefined,

          delivery: {
            connect: args.data.delivery,
          },

          driver: {
            connect: args.data.driver,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DeliveryAssignment)
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "delete",
    possession: "any",
  })
  async deleteDeliveryAssignment(
    @graphql.Args() args: DeleteDeliveryAssignmentArgs
  ): Promise<DeliveryAssignment | null> {
    try {
      return await this.service.deleteDeliveryAssignment(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "assignedBy",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getAssignedBy(
    @graphql.Parent() parent: DeliveryAssignment
  ): Promise<User | null> {
    const result = await this.service.getAssignedBy(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "delivery",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getDelivery(
    @graphql.Parent() parent: DeliveryAssignment
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getDelivery(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Driver, {
    nullable: true,
    name: "driver",
  })
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async getDriver(
    @graphql.Parent() parent: DeliveryAssignment
  ): Promise<Driver | null> {
    const result = await this.service.getDriver(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
