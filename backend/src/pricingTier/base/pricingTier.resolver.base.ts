/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { PricingTier } from "./PricingTier";
import { PricingTierCountArgs } from "./PricingTierCountArgs";
import { PricingTierFindManyArgs } from "./PricingTierFindManyArgs";
import { PricingTierFindUniqueArgs } from "./PricingTierFindUniqueArgs";
import { CreatePricingTierArgs } from "./CreatePricingTierArgs";
import { UpdatePricingTierArgs } from "./UpdatePricingTierArgs";
import { DeletePricingTierArgs } from "./DeletePricingTierArgs";
import { PricingConfig } from "../../pricingConfig/base/PricingConfig";
import { PricingTierService } from "../pricingTier.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PricingTier)
export class PricingTierResolverBase {
  constructor(
    protected readonly service: PricingTierService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "read",
    possession: "any",
  })
  async _pricingTiersMeta(
    @graphql.Args() args: PricingTierCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PricingTier])
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "read",
    possession: "any",
  })
  async pricingTiers(
    @graphql.Args() args: PricingTierFindManyArgs
  ): Promise<PricingTier[]> {
    return this.service.pricingTiers(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PricingTier, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "read",
    possession: "own",
  })
  async pricingTier(
    @graphql.Args() args: PricingTierFindUniqueArgs
  ): Promise<PricingTier | null> {
    const result = await this.service.pricingTier(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => PricingTier)
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "create",
    possession: "any",
  })
  async createPricingTier(
    @graphql.Args() args: CreatePricingTierArgs
  ): Promise<PricingTier> {
    return await this.service.createPricingTier({
      ...args,
      data: {
        ...args.data,

        pricingConfig: {
          connect: args.data.pricingConfig,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => PricingTier)
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "update",
    possession: "any",
  })
  async updatePricingTier(
    @graphql.Args() args: UpdatePricingTierArgs
  ): Promise<PricingTier | null> {
    try {
      return await this.service.updatePricingTier({
        ...args,
        data: {
          ...args.data,

          pricingConfig: {
            connect: args.data.pricingConfig,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => PricingTier)
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "delete",
    possession: "any",
  })
  async deletePricingTier(
    @graphql.Args() args: DeletePricingTierArgs
  ): Promise<PricingTier | null> {
    try {
      return await this.service.deletePricingTier(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => PricingConfig, {
    nullable: true,
    name: "pricingConfig",
  })
  @nestAccessControl.UseRoles({
    resource: "PricingConfig",
    action: "read",
    possession: "any",
  })
  async getPricingConfig(
    @graphql.Parent() parent: PricingTier
  ): Promise<PricingConfig | null> {
    const result = await this.service.getPricingConfig(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
