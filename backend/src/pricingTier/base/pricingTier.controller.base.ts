/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PricingTierService } from "../pricingTier.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PricingTierCreateInput } from "./PricingTierCreateInput";
import { PricingTier } from "./PricingTier";
import { PricingTierFindManyArgs } from "./PricingTierFindManyArgs";
import { PricingTierWhereUniqueInput } from "./PricingTierWhereUniqueInput";
import { PricingTierUpdateInput } from "./PricingTierUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PricingTierControllerBase {
  constructor(
    protected readonly service: PricingTierService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PricingTier })
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPricingTier(
    @common.Body() data: PricingTierCreateInput
  ): Promise<PricingTier> {
    return await this.service.createPricingTier({
      data: {
        ...data,

        pricingConfig: {
          connect: data.pricingConfig,
        },
      },
      select: {
        createdAt: true,
        flatPrice: true,
        id: true,
        maxMiles: true,
        minMiles: true,

        pricingConfig: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PricingTier] })
  @ApiNestedQuery(PricingTierFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async pricingTiers(@common.Req() request: Request): Promise<PricingTier[]> {
    const args = plainToClass(PricingTierFindManyArgs, request.query);
    return this.service.pricingTiers({
      ...args,
      select: {
        createdAt: true,
        flatPrice: true,
        id: true,
        maxMiles: true,
        minMiles: true,

        pricingConfig: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PricingTier })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async pricingTier(
    @common.Param() params: PricingTierWhereUniqueInput
  ): Promise<PricingTier | null> {
    const result = await this.service.pricingTier({
      where: params,
      select: {
        createdAt: true,
        flatPrice: true,
        id: true,
        maxMiles: true,
        minMiles: true,

        pricingConfig: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PricingTier })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePricingTier(
    @common.Param() params: PricingTierWhereUniqueInput,
    @common.Body() data: PricingTierUpdateInput
  ): Promise<PricingTier | null> {
    try {
      return await this.service.updatePricingTier({
        where: params,
        data: {
          ...data,

          pricingConfig: {
            connect: data.pricingConfig,
          },
        },
        select: {
          createdAt: true,
          flatPrice: true,
          id: true,
          maxMiles: true,
          minMiles: true,

          pricingConfig: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PricingTier })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PricingTier",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePricingTier(
    @common.Param() params: PricingTierWhereUniqueInput
  ): Promise<PricingTier | null> {
    try {
      return await this.service.deletePricingTier({
        where: params,
        select: {
          createdAt: true,
          flatPrice: true,
          id: true,
          maxMiles: true,
          minMiles: true,

          pricingConfig: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
