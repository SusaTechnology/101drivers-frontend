/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DriverDistrictPreferenceService } from "../driverDistrictPreference.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DriverDistrictPreferenceCreateInput } from "./DriverDistrictPreferenceCreateInput";
import { DriverDistrictPreference } from "./DriverDistrictPreference";
import { DriverDistrictPreferenceFindManyArgs } from "./DriverDistrictPreferenceFindManyArgs";
import { DriverDistrictPreferenceWhereUniqueInput } from "./DriverDistrictPreferenceWhereUniqueInput";
import { DriverDistrictPreferenceUpdateInput } from "./DriverDistrictPreferenceUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DriverDistrictPreferenceControllerBase {
  constructor(
    protected readonly service: DriverDistrictPreferenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DriverDistrictPreference })
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDriverDistrictPreference(
    @common.Body() data: DriverDistrictPreferenceCreateInput
  ): Promise<DriverDistrictPreference> {
    return await this.service.createDriverDistrictPreference({
      data: {
        ...data,

        district: {
          connect: data.district,
        },

        driver: {
          connect: data.driver,
        },
      },
      select: {
        createdAt: true,

        district: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DriverDistrictPreference] })
  @ApiNestedQuery(DriverDistrictPreferenceFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async driverDistrictPreferences(
    @common.Req() request: Request
  ): Promise<DriverDistrictPreference[]> {
    const args = plainToClass(
      DriverDistrictPreferenceFindManyArgs,
      request.query
    );
    return this.service.driverDistrictPreferences({
      ...args,
      select: {
        createdAt: true,

        district: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DriverDistrictPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async driverDistrictPreference(
    @common.Param() params: DriverDistrictPreferenceWhereUniqueInput
  ): Promise<DriverDistrictPreference | null> {
    const result = await this.service.driverDistrictPreference({
      where: params,
      select: {
        createdAt: true,

        district: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DriverDistrictPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDriverDistrictPreference(
    @common.Param() params: DriverDistrictPreferenceWhereUniqueInput,
    @common.Body() data: DriverDistrictPreferenceUpdateInput
  ): Promise<DriverDistrictPreference | null> {
    try {
      return await this.service.updateDriverDistrictPreference({
        where: params,
        data: {
          ...data,

          district: {
            connect: data.district,
          },

          driver: {
            connect: data.driver,
          },
        },
        select: {
          createdAt: true,

          district: {
            select: {
              id: true,
            },
          },

          driver: {
            select: {
              id: true,
            },
          },

          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DriverDistrictPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDriverDistrictPreference(
    @common.Param() params: DriverDistrictPreferenceWhereUniqueInput
  ): Promise<DriverDistrictPreference | null> {
    try {
      return await this.service.deleteDriverDistrictPreference({
        where: params,
        select: {
          createdAt: true,

          district: {
            select: {
              id: true,
            },
          },

          driver: {
            select: {
              id: true,
            },
          },

          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
