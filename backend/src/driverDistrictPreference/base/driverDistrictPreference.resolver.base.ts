/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DriverDistrictPreference } from "./DriverDistrictPreference";
import { DriverDistrictPreferenceCountArgs } from "./DriverDistrictPreferenceCountArgs";
import { DriverDistrictPreferenceFindManyArgs } from "./DriverDistrictPreferenceFindManyArgs";
import { DriverDistrictPreferenceFindUniqueArgs } from "./DriverDistrictPreferenceFindUniqueArgs";
import { CreateDriverDistrictPreferenceArgs } from "./CreateDriverDistrictPreferenceArgs";
import { UpdateDriverDistrictPreferenceArgs } from "./UpdateDriverDistrictPreferenceArgs";
import { DeleteDriverDistrictPreferenceArgs } from "./DeleteDriverDistrictPreferenceArgs";
import { ServiceDistrict } from "../../serviceDistrict/base/ServiceDistrict";
import { Driver } from "../../driver/base/Driver";
import { DriverDistrictPreferenceService } from "../driverDistrictPreference.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DriverDistrictPreference)
export class DriverDistrictPreferenceResolverBase {
  constructor(
    protected readonly service: DriverDistrictPreferenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "read",
    possession: "any",
  })
  async _driverDistrictPreferencesMeta(
    @graphql.Args() args: DriverDistrictPreferenceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DriverDistrictPreference])
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "read",
    possession: "any",
  })
  async driverDistrictPreferences(
    @graphql.Args() args: DriverDistrictPreferenceFindManyArgs
  ): Promise<DriverDistrictPreference[]> {
    return this.service.driverDistrictPreferences(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DriverDistrictPreference, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "read",
    possession: "own",
  })
  async driverDistrictPreference(
    @graphql.Args() args: DriverDistrictPreferenceFindUniqueArgs
  ): Promise<DriverDistrictPreference | null> {
    const result = await this.service.driverDistrictPreference(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverDistrictPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "create",
    possession: "any",
  })
  async createDriverDistrictPreference(
    @graphql.Args() args: CreateDriverDistrictPreferenceArgs
  ): Promise<DriverDistrictPreference> {
    return await this.service.createDriverDistrictPreference({
      ...args,
      data: {
        ...args.data,

        district: {
          connect: args.data.district,
        },

        driver: {
          connect: args.data.driver,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverDistrictPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "update",
    possession: "any",
  })
  async updateDriverDistrictPreference(
    @graphql.Args() args: UpdateDriverDistrictPreferenceArgs
  ): Promise<DriverDistrictPreference | null> {
    try {
      return await this.service.updateDriverDistrictPreference({
        ...args,
        data: {
          ...args.data,

          district: {
            connect: args.data.district,
          },

          driver: {
            connect: args.data.driver,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DriverDistrictPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "delete",
    possession: "any",
  })
  async deleteDriverDistrictPreference(
    @graphql.Args() args: DeleteDriverDistrictPreferenceArgs
  ): Promise<DriverDistrictPreference | null> {
    try {
      return await this.service.deleteDriverDistrictPreference(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => ServiceDistrict, {
    nullable: true,
    name: "district",
  })
  @nestAccessControl.UseRoles({
    resource: "ServiceDistrict",
    action: "read",
    possession: "any",
  })
  async getDistrict(
    @graphql.Parent() parent: DriverDistrictPreference
  ): Promise<ServiceDistrict | null> {
    const result = await this.service.getDistrict(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Driver, {
    nullable: true,
    name: "driver",
  })
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async getDriver(
    @graphql.Parent() parent: DriverDistrictPreference
  ): Promise<Driver | null> {
    const result = await this.service.getDriver(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
