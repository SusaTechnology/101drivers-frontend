/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { InvestorLead } from "./InvestorLead";
import { InvestorLeadCountArgs } from "./InvestorLeadCountArgs";
import { InvestorLeadFindManyArgs } from "./InvestorLeadFindManyArgs";
import { InvestorLeadFindUniqueArgs } from "./InvestorLeadFindUniqueArgs";
import { CreateInvestorLeadArgs } from "./CreateInvestorLeadArgs";
import { UpdateInvestorLeadArgs } from "./UpdateInvestorLeadArgs";
import { DeleteInvestorLeadArgs } from "./DeleteInvestorLeadArgs";
import { InvestorLeadService } from "../investorLead.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => InvestorLead)
export class InvestorLeadResolverBase {
  constructor(
    protected readonly service: InvestorLeadService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "InvestorLead",
    action: "read",
    possession: "any",
  })
  async _investorLeadsMeta(
    @graphql.Args() args: InvestorLeadCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [InvestorLead])
  @nestAccessControl.UseRoles({
    resource: "InvestorLead",
    action: "read",
    possession: "any",
  })
  async investorLeads(
    @graphql.Args() args: InvestorLeadFindManyArgs
  ): Promise<InvestorLead[]> {
    return this.service.investorLeads(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => InvestorLead, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "InvestorLead",
    action: "read",
    possession: "own",
  })
  async investorLead(
    @graphql.Args() args: InvestorLeadFindUniqueArgs
  ): Promise<InvestorLead | null> {
    const result = await this.service.investorLead(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => InvestorLead)
  @nestAccessControl.UseRoles({
    resource: "InvestorLead",
    action: "create",
    possession: "any",
  })
  async createInvestorLead(
    @graphql.Args() args: CreateInvestorLeadArgs
  ): Promise<InvestorLead> {
    return await this.service.createInvestorLead({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => InvestorLead)
  @nestAccessControl.UseRoles({
    resource: "InvestorLead",
    action: "update",
    possession: "any",
  })
  async updateInvestorLead(
    @graphql.Args() args: UpdateInvestorLeadArgs
  ): Promise<InvestorLead | null> {
    try {
      return await this.service.updateInvestorLead({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => InvestorLead)
  @nestAccessControl.UseRoles({
    resource: "InvestorLead",
    action: "delete",
    possession: "any",
  })
  async deleteInvestorLead(
    @graphql.Args() args: DeleteInvestorLeadArgs
  ): Promise<InvestorLead | null> {
    try {
      return await this.service.deleteInvestorLead(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
