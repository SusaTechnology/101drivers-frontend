/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { Customer } from "./Customer";
import { CustomerCountArgs } from "./CustomerCountArgs";
import { CustomerFindManyArgs } from "./CustomerFindManyArgs";
import { CustomerFindUniqueArgs } from "./CustomerFindUniqueArgs";
import { CreateCustomerArgs } from "./CreateCustomerArgs";
import { UpdateCustomerArgs } from "./UpdateCustomerArgs";
import { DeleteCustomerArgs } from "./DeleteCustomerArgs";
import { SavedAddressFindManyArgs } from "../../savedAddress/base/SavedAddressFindManyArgs";
import { SavedAddress } from "../../savedAddress/base/SavedAddress";
import { AdminAuditLogFindManyArgs } from "../../adminAuditLog/base/AdminAuditLogFindManyArgs";
import { AdminAuditLog } from "../../adminAuditLog/base/AdminAuditLog";
import { DeliveryRequestFindManyArgs } from "../../deliveryRequest/base/DeliveryRequestFindManyArgs";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { NotificationEventFindManyArgs } from "../../notificationEvent/base/NotificationEventFindManyArgs";
import { NotificationEvent } from "../../notificationEvent/base/NotificationEvent";
import { DeliveryRatingFindManyArgs } from "../../deliveryRating/base/DeliveryRatingFindManyArgs";
import { DeliveryRating } from "../../deliveryRating/base/DeliveryRating";
import { SavedVehicleFindManyArgs } from "../../savedVehicle/base/SavedVehicleFindManyArgs";
import { SavedVehicle } from "../../savedVehicle/base/SavedVehicle";
import { User } from "../../user/base/User";
import { PricingConfig } from "../../pricingConfig/base/PricingConfig";
import { CustomerService } from "../customer.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Customer)
export class CustomerResolverBase {
  constructor(
    protected readonly service: CustomerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async _customersMeta(
    @graphql.Args() args: CustomerCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Customer])
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async customers(
    @graphql.Args() args: CustomerFindManyArgs
  ): Promise<Customer[]> {
    return this.service.customers(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Customer, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "own",
  })
  async customer(
    @graphql.Args() args: CustomerFindUniqueArgs
  ): Promise<Customer | null> {
    const result = await this.service.customer(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Customer)
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "create",
    possession: "any",
  })
  async createCustomer(
    @graphql.Args() args: CreateCustomerArgs
  ): Promise<Customer> {
    return await this.service.createCustomer({
      ...args,
      data: {
        ...args.data,

        approvedBy: args.data.approvedBy
          ? {
              connect: args.data.approvedBy,
            }
          : undefined,

        defaultPickup: args.data.defaultPickup
          ? {
              connect: args.data.defaultPickup,
            }
          : undefined,

        pricingConfig: args.data.pricingConfig
          ? {
              connect: args.data.pricingConfig,
            }
          : undefined,

        user: {
          connect: args.data.user,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Customer)
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "update",
    possession: "any",
  })
  async updateCustomer(
    @graphql.Args() args: UpdateCustomerArgs
  ): Promise<Customer | null> {
    try {
      return await this.service.updateCustomer({
        ...args,
        data: {
          ...args.data,

          approvedBy: args.data.approvedBy
            ? {
                connect: args.data.approvedBy,
              }
            : undefined,

          defaultPickup: args.data.defaultPickup
            ? {
                connect: args.data.defaultPickup,
              }
            : undefined,

          pricingConfig: args.data.pricingConfig
            ? {
                connect: args.data.pricingConfig,
              }
            : undefined,

          user: {
            connect: args.data.user,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Customer)
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "delete",
    possession: "any",
  })
  async deleteCustomer(
    @graphql.Args() args: DeleteCustomerArgs
  ): Promise<Customer | null> {
    try {
      return await this.service.deleteCustomer(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [SavedAddress], { name: "addresses" })
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "read",
    possession: "any",
  })
  async findAddresses(
    @graphql.Parent() parent: Customer,
    @graphql.Args() args: SavedAddressFindManyArgs
  ): Promise<SavedAddress[]> {
    const results = await this.service.findAddresses(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [AdminAuditLog], { name: "audits" })
  @nestAccessControl.UseRoles({
    resource: "AdminAuditLog",
    action: "read",
    possession: "any",
  })
  async findAudits(
    @graphql.Parent() parent: Customer,
    @graphql.Args() args: AdminAuditLogFindManyArgs
  ): Promise<AdminAuditLog[]> {
    const results = await this.service.findAudits(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryRequest], { name: "deliveries" })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async findDeliveries(
    @graphql.Parent() parent: Customer,
    @graphql.Args() args: DeliveryRequestFindManyArgs
  ): Promise<DeliveryRequest[]> {
    const results = await this.service.findDeliveries(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [NotificationEvent], { name: "notifications" })
  @nestAccessControl.UseRoles({
    resource: "NotificationEvent",
    action: "read",
    possession: "any",
  })
  async findNotifications(
    @graphql.Parent() parent: Customer,
    @graphql.Args() args: NotificationEventFindManyArgs
  ): Promise<NotificationEvent[]> {
    const results = await this.service.findNotifications(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryRating], { name: "ratings" })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "read",
    possession: "any",
  })
  async findRatings(
    @graphql.Parent() parent: Customer,
    @graphql.Args() args: DeliveryRatingFindManyArgs
  ): Promise<DeliveryRating[]> {
    const results = await this.service.findRatings(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [SavedVehicle], { name: "vehicles" })
  @nestAccessControl.UseRoles({
    resource: "SavedVehicle",
    action: "read",
    possession: "any",
  })
  async findVehicles(
    @graphql.Parent() parent: Customer,
    @graphql.Args() args: SavedVehicleFindManyArgs
  ): Promise<SavedVehicle[]> {
    const results = await this.service.findVehicles(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "approvedBy",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getApprovedBy(
    @graphql.Parent() parent: Customer
  ): Promise<User | null> {
    const result = await this.service.getApprovedBy(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => SavedAddress, {
    nullable: true,
    name: "defaultPickup",
  })
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "read",
    possession: "any",
  })
  async getDefaultPickup(
    @graphql.Parent() parent: Customer
  ): Promise<SavedAddress | null> {
    const result = await this.service.getDefaultPickup(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => PricingConfig, {
    nullable: true,
    name: "pricingConfig",
  })
  @nestAccessControl.UseRoles({
    resource: "PricingConfig",
    action: "read",
    possession: "any",
  })
  async getPricingConfig(
    @graphql.Parent() parent: Customer
  ): Promise<PricingConfig | null> {
    const result = await this.service.getPricingConfig(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getUser(@graphql.Parent() parent: Customer): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
