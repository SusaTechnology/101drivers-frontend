/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { ServiceDistrict } from "./ServiceDistrict";
import { ServiceDistrictCountArgs } from "./ServiceDistrictCountArgs";
import { ServiceDistrictFindManyArgs } from "./ServiceDistrictFindManyArgs";
import { ServiceDistrictFindUniqueArgs } from "./ServiceDistrictFindUniqueArgs";
import { CreateServiceDistrictArgs } from "./CreateServiceDistrictArgs";
import { UpdateServiceDistrictArgs } from "./UpdateServiceDistrictArgs";
import { DeleteServiceDistrictArgs } from "./DeleteServiceDistrictArgs";
import { DriverDistrictPreferenceFindManyArgs } from "../../driverDistrictPreference/base/DriverDistrictPreferenceFindManyArgs";
import { DriverDistrictPreference } from "../../driverDistrictPreference/base/DriverDistrictPreference";
import { ServiceDistrictService } from "../serviceDistrict.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ServiceDistrict)
export class ServiceDistrictResolverBase {
  constructor(
    protected readonly service: ServiceDistrictService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ServiceDistrict",
    action: "read",
    possession: "any",
  })
  async _serviceDistrictsMeta(
    @graphql.Args() args: ServiceDistrictCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ServiceDistrict])
  @nestAccessControl.UseRoles({
    resource: "ServiceDistrict",
    action: "read",
    possession: "any",
  })
  async serviceDistricts(
    @graphql.Args() args: ServiceDistrictFindManyArgs
  ): Promise<ServiceDistrict[]> {
    return this.service.serviceDistricts(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ServiceDistrict, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ServiceDistrict",
    action: "read",
    possession: "own",
  })
  async serviceDistrict(
    @graphql.Args() args: ServiceDistrictFindUniqueArgs
  ): Promise<ServiceDistrict | null> {
    const result = await this.service.serviceDistrict(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ServiceDistrict)
  @nestAccessControl.UseRoles({
    resource: "ServiceDistrict",
    action: "create",
    possession: "any",
  })
  async createServiceDistrict(
    @graphql.Args() args: CreateServiceDistrictArgs
  ): Promise<ServiceDistrict> {
    return await this.service.createServiceDistrict({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ServiceDistrict)
  @nestAccessControl.UseRoles({
    resource: "ServiceDistrict",
    action: "update",
    possession: "any",
  })
  async updateServiceDistrict(
    @graphql.Args() args: UpdateServiceDistrictArgs
  ): Promise<ServiceDistrict | null> {
    try {
      return await this.service.updateServiceDistrict({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => ServiceDistrict)
  @nestAccessControl.UseRoles({
    resource: "ServiceDistrict",
    action: "delete",
    possession: "any",
  })
  async deleteServiceDistrict(
    @graphql.Args() args: DeleteServiceDistrictArgs
  ): Promise<ServiceDistrict | null> {
    try {
      return await this.service.deleteServiceDistrict(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DriverDistrictPreference], {
    name: "driverPrefs",
  })
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "read",
    possession: "any",
  })
  async findDriverPrefs(
    @graphql.Parent() parent: ServiceDistrict,
    @graphql.Args() args: DriverDistrictPreferenceFindManyArgs
  ): Promise<DriverDistrictPreference[]> {
    const results = await this.service.findDriverPrefs(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }
}
