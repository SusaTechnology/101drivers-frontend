/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DriverPayoutService } from "../driverPayout.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DriverPayoutCreateInput } from "./DriverPayoutCreateInput";
import { DriverPayout } from "./DriverPayout";
import { DriverPayoutFindManyArgs } from "./DriverPayoutFindManyArgs";
import { DriverPayoutWhereUniqueInput } from "./DriverPayoutWhereUniqueInput";
import { DriverPayoutUpdateInput } from "./DriverPayoutUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DriverPayoutControllerBase {
  constructor(
    protected readonly service: DriverPayoutService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DriverPayout })
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDriverPayout(
    @common.Body() data: DriverPayoutCreateInput
  ): Promise<DriverPayout> {
    return await this.service.createDriverPayout({
      data: {
        ...data,

        delivery: {
          connect: data.delivery,
        },

        driver: {
          connect: data.driver,
        },
      },
      select: {
        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        driverSharePct: true,
        failedAt: true,
        failureMessage: true,
        grossAmount: true,
        id: true,
        insuranceFee: true,
        netAmount: true,
        paidAt: true,
        platformFee: true,
        providerTransferId: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DriverPayout] })
  @ApiNestedQuery(DriverPayoutFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async driverPayouts(@common.Req() request: Request): Promise<DriverPayout[]> {
    const args = plainToClass(DriverPayoutFindManyArgs, request.query);
    return this.service.driverPayouts({
      ...args,
      select: {
        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        driverSharePct: true,
        failedAt: true,
        failureMessage: true,
        grossAmount: true,
        id: true,
        insuranceFee: true,
        netAmount: true,
        paidAt: true,
        platformFee: true,
        providerTransferId: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DriverPayout })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async driverPayout(
    @common.Param() params: DriverPayoutWhereUniqueInput
  ): Promise<DriverPayout | null> {
    const result = await this.service.driverPayout({
      where: params,
      select: {
        createdAt: true,

        delivery: {
          select: {
            id: true,
          },
        },

        driver: {
          select: {
            id: true,
          },
        },

        driverSharePct: true,
        failedAt: true,
        failureMessage: true,
        grossAmount: true,
        id: true,
        insuranceFee: true,
        netAmount: true,
        paidAt: true,
        platformFee: true,
        providerTransferId: true,
        status: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DriverPayout })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDriverPayout(
    @common.Param() params: DriverPayoutWhereUniqueInput,
    @common.Body() data: DriverPayoutUpdateInput
  ): Promise<DriverPayout | null> {
    try {
      return await this.service.updateDriverPayout({
        where: params,
        data: {
          ...data,

          delivery: {
            connect: data.delivery,
          },

          driver: {
            connect: data.driver,
          },
        },
        select: {
          createdAt: true,

          delivery: {
            select: {
              id: true,
            },
          },

          driver: {
            select: {
              id: true,
            },
          },

          driverSharePct: true,
          failedAt: true,
          failureMessage: true,
          grossAmount: true,
          id: true,
          insuranceFee: true,
          netAmount: true,
          paidAt: true,
          platformFee: true,
          providerTransferId: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DriverPayout })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDriverPayout(
    @common.Param() params: DriverPayoutWhereUniqueInput
  ): Promise<DriverPayout | null> {
    try {
      return await this.service.deleteDriverPayout({
        where: params,
        select: {
          createdAt: true,

          delivery: {
            select: {
              id: true,
            },
          },

          driver: {
            select: {
              id: true,
            },
          },

          driverSharePct: true,
          failedAt: true,
          failureMessage: true,
          grossAmount: true,
          id: true,
          insuranceFee: true,
          netAmount: true,
          paidAt: true,
          platformFee: true,
          providerTransferId: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
