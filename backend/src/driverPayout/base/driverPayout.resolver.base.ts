/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DriverPayout } from "./DriverPayout";
import { DriverPayoutCountArgs } from "./DriverPayoutCountArgs";
import { DriverPayoutFindManyArgs } from "./DriverPayoutFindManyArgs";
import { DriverPayoutFindUniqueArgs } from "./DriverPayoutFindUniqueArgs";
import { CreateDriverPayoutArgs } from "./CreateDriverPayoutArgs";
import { UpdateDriverPayoutArgs } from "./UpdateDriverPayoutArgs";
import { DeleteDriverPayoutArgs } from "./DeleteDriverPayoutArgs";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { Driver } from "../../driver/base/Driver";
import { DriverPayoutService } from "../driverPayout.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DriverPayout)
export class DriverPayoutResolverBase {
  constructor(
    protected readonly service: DriverPayoutService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "read",
    possession: "any",
  })
  async _driverPayoutsMeta(
    @graphql.Args() args: DriverPayoutCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DriverPayout])
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "read",
    possession: "any",
  })
  async driverPayouts(
    @graphql.Args() args: DriverPayoutFindManyArgs
  ): Promise<DriverPayout[]> {
    return this.service.driverPayouts(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DriverPayout, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "read",
    possession: "own",
  })
  async driverPayout(
    @graphql.Args() args: DriverPayoutFindUniqueArgs
  ): Promise<DriverPayout | null> {
    const result = await this.service.driverPayout(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverPayout)
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "create",
    possession: "any",
  })
  async createDriverPayout(
    @graphql.Args() args: CreateDriverPayoutArgs
  ): Promise<DriverPayout> {
    return await this.service.createDriverPayout({
      ...args,
      data: {
        ...args.data,

        delivery: {
          connect: args.data.delivery,
        },

        driver: {
          connect: args.data.driver,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverPayout)
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "update",
    possession: "any",
  })
  async updateDriverPayout(
    @graphql.Args() args: UpdateDriverPayoutArgs
  ): Promise<DriverPayout | null> {
    try {
      return await this.service.updateDriverPayout({
        ...args,
        data: {
          ...args.data,

          delivery: {
            connect: args.data.delivery,
          },

          driver: {
            connect: args.data.driver,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DriverPayout)
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "delete",
    possession: "any",
  })
  async deleteDriverPayout(
    @graphql.Args() args: DeleteDriverPayoutArgs
  ): Promise<DriverPayout | null> {
    try {
      return await this.service.deleteDriverPayout(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "delivery",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getDelivery(
    @graphql.Parent() parent: DriverPayout
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getDelivery(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Driver, {
    nullable: true,
    name: "driver",
  })
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async getDriver(
    @graphql.Parent() parent: DriverPayout
  ): Promise<Driver | null> {
    const result = await this.service.getDriver(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
