/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DeliveryRequest } from "./DeliveryRequest";
import { DeliveryRequestCountArgs } from "./DeliveryRequestCountArgs";
import { DeliveryRequestFindManyArgs } from "./DeliveryRequestFindManyArgs";
import { DeliveryRequestFindUniqueArgs } from "./DeliveryRequestFindUniqueArgs";
import { CreateDeliveryRequestArgs } from "./CreateDeliveryRequestArgs";
import { UpdateDeliveryRequestArgs } from "./UpdateDeliveryRequestArgs";
import { DeleteDeliveryRequestArgs } from "./DeleteDeliveryRequestArgs";
import { DeliveryAssignmentFindManyArgs } from "../../deliveryAssignment/base/DeliveryAssignmentFindManyArgs";
import { DeliveryAssignment } from "../../deliveryAssignment/base/DeliveryAssignment";
import { AdminAuditLogFindManyArgs } from "../../adminAuditLog/base/AdminAuditLogFindManyArgs";
import { AdminAuditLog } from "../../adminAuditLog/base/AdminAuditLog";
import { DeliveryEvidenceFindManyArgs } from "../../deliveryEvidence/base/DeliveryEvidenceFindManyArgs";
import { DeliveryEvidence } from "../../deliveryEvidence/base/DeliveryEvidence";
import { EvidenceExportFindManyArgs } from "../../evidenceExport/base/EvidenceExportFindManyArgs";
import { EvidenceExport } from "../../evidenceExport/base/EvidenceExport";
import { NotificationEventFindManyArgs } from "../../notificationEvent/base/NotificationEventFindManyArgs";
import { NotificationEvent } from "../../notificationEvent/base/NotificationEvent";
import { ScheduleChangeRequestFindManyArgs } from "../../scheduleChangeRequest/base/ScheduleChangeRequestFindManyArgs";
import { ScheduleChangeRequest } from "../../scheduleChangeRequest/base/ScheduleChangeRequest";
import { DeliveryStatusHistoryFindManyArgs } from "../../deliveryStatusHistory/base/DeliveryStatusHistoryFindManyArgs";
import { DeliveryStatusHistory } from "../../deliveryStatusHistory/base/DeliveryStatusHistory";
import { SupportRequestFindManyArgs } from "../../supportRequest/base/SupportRequestFindManyArgs";
import { SupportRequest } from "../../supportRequest/base/SupportRequest";
import { DeliveryCompliance } from "../../deliveryCompliance/base/DeliveryCompliance";
import { User } from "../../user/base/User";
import { Customer } from "../../customer/base/Customer";
import { DisputeCase } from "../../disputeCase/base/DisputeCase";
import { Payment } from "../../payment/base/Payment";
import { DriverPayout } from "../../driverPayout/base/DriverPayout";
import { Quote } from "../../quote/base/Quote";
import { DeliveryRating } from "../../deliveryRating/base/DeliveryRating";
import { Tip } from "../../tip/base/Tip";
import { TrackingSession } from "../../trackingSession/base/TrackingSession";
import { DeliveryRequestService } from "../deliveryRequest.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DeliveryRequest)
export class DeliveryRequestResolverBase {
  constructor(
    protected readonly service: DeliveryRequestService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async _deliveryRequestsMeta(
    @graphql.Args() args: DeliveryRequestCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DeliveryRequest])
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async deliveryRequests(
    @graphql.Args() args: DeliveryRequestFindManyArgs
  ): Promise<DeliveryRequest[]> {
    return this.service.deliveryRequests(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DeliveryRequest, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "own",
  })
  async deliveryRequest(
    @graphql.Args() args: DeliveryRequestFindUniqueArgs
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.deliveryRequest(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryRequest)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "create",
    possession: "any",
  })
  async createDeliveryRequest(
    @graphql.Args() args: CreateDeliveryRequestArgs
  ): Promise<DeliveryRequest> {
    return await this.service.createDeliveryRequest({
      ...args,
      data: {
        ...args.data,

        compliance: args.data.compliance
          ? {
              connect: args.data.compliance,
            }
          : undefined,

        createdBy: args.data.createdBy
          ? {
              connect: args.data.createdBy,
            }
          : undefined,

        customer: {
          connect: args.data.customer,
        },

        dispute: args.data.dispute
          ? {
              connect: args.data.dispute,
            }
          : undefined,

        payment: args.data.payment
          ? {
              connect: args.data.payment,
            }
          : undefined,

        payout: args.data.payout
          ? {
              connect: args.data.payout,
            }
          : undefined,

        quote: args.data.quote
          ? {
              connect: args.data.quote,
            }
          : undefined,

        rating: args.data.rating
          ? {
              connect: args.data.rating,
            }
          : undefined,

        resubmittedFrom: args.data.resubmittedFrom
          ? {
              connect: args.data.resubmittedFrom,
            }
          : undefined,

        tip: args.data.tip
          ? {
              connect: args.data.tip,
            }
          : undefined,

        trackingSession: args.data.trackingSession
          ? {
              connect: args.data.trackingSession,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryRequest)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "update",
    possession: "any",
  })
  async updateDeliveryRequest(
    @graphql.Args() args: UpdateDeliveryRequestArgs
  ): Promise<DeliveryRequest | null> {
    try {
      return await this.service.updateDeliveryRequest({
        ...args,
        data: {
          ...args.data,

          compliance: args.data.compliance
            ? {
                connect: args.data.compliance,
              }
            : undefined,

          createdBy: args.data.createdBy
            ? {
                connect: args.data.createdBy,
              }
            : undefined,

          customer: {
            connect: args.data.customer,
          },

          dispute: args.data.dispute
            ? {
                connect: args.data.dispute,
              }
            : undefined,

          payment: args.data.payment
            ? {
                connect: args.data.payment,
              }
            : undefined,

          payout: args.data.payout
            ? {
                connect: args.data.payout,
              }
            : undefined,

          quote: args.data.quote
            ? {
                connect: args.data.quote,
              }
            : undefined,

          rating: args.data.rating
            ? {
                connect: args.data.rating,
              }
            : undefined,

          resubmittedFrom: args.data.resubmittedFrom
            ? {
                connect: args.data.resubmittedFrom,
              }
            : undefined,

          tip: args.data.tip
            ? {
                connect: args.data.tip,
              }
            : undefined,

          trackingSession: args.data.trackingSession
            ? {
                connect: args.data.trackingSession,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DeliveryRequest)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "delete",
    possession: "any",
  })
  async deleteDeliveryRequest(
    @graphql.Args() args: DeleteDeliveryRequestArgs
  ): Promise<DeliveryRequest | null> {
    try {
      return await this.service.deleteDeliveryRequest(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryAssignment], { name: "assignments" })
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "read",
    possession: "any",
  })
  async findAssignments(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: DeliveryAssignmentFindManyArgs
  ): Promise<DeliveryAssignment[]> {
    const results = await this.service.findAssignments(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [AdminAuditLog], { name: "audits" })
  @nestAccessControl.UseRoles({
    resource: "AdminAuditLog",
    action: "read",
    possession: "any",
  })
  async findAudits(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: AdminAuditLogFindManyArgs
  ): Promise<AdminAuditLog[]> {
    const results = await this.service.findAudits(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryEvidence], { name: "evidence" })
  @nestAccessControl.UseRoles({
    resource: "DeliveryEvidence",
    action: "read",
    possession: "any",
  })
  async findEvidence(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: DeliveryEvidenceFindManyArgs
  ): Promise<DeliveryEvidence[]> {
    const results = await this.service.findEvidence(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [EvidenceExport], { name: "evidenceExports" })
  @nestAccessControl.UseRoles({
    resource: "EvidenceExport",
    action: "read",
    possession: "any",
  })
  async findEvidenceExports(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: EvidenceExportFindManyArgs
  ): Promise<EvidenceExport[]> {
    const results = await this.service.findEvidenceExports(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [NotificationEvent], { name: "notifications" })
  @nestAccessControl.UseRoles({
    resource: "NotificationEvent",
    action: "read",
    possession: "any",
  })
  async findNotifications(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: NotificationEventFindManyArgs
  ): Promise<NotificationEvent[]> {
    const results = await this.service.findNotifications(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryRequest], { name: "resubmissions" })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async findResubmissions(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: DeliveryRequestFindManyArgs
  ): Promise<DeliveryRequest[]> {
    const results = await this.service.findResubmissions(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [ScheduleChangeRequest], {
    name: "scheduleChanges",
  })
  @nestAccessControl.UseRoles({
    resource: "ScheduleChangeRequest",
    action: "read",
    possession: "any",
  })
  async findScheduleChanges(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: ScheduleChangeRequestFindManyArgs
  ): Promise<ScheduleChangeRequest[]> {
    const results = await this.service.findScheduleChanges(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryStatusHistory], {
    name: "statusHistory",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryStatusHistory",
    action: "read",
    possession: "any",
  })
  async findStatusHistory(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: DeliveryStatusHistoryFindManyArgs
  ): Promise<DeliveryStatusHistory[]> {
    const results = await this.service.findStatusHistory(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [SupportRequest], { name: "supportRequests" })
  @nestAccessControl.UseRoles({
    resource: "SupportRequest",
    action: "read",
    possession: "any",
  })
  async findSupportRequests(
    @graphql.Parent() parent: DeliveryRequest,
    @graphql.Args() args: SupportRequestFindManyArgs
  ): Promise<SupportRequest[]> {
    const results = await this.service.findSupportRequests(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryCompliance, {
    nullable: true,
    name: "compliance",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryCompliance",
    action: "read",
    possession: "any",
  })
  async getCompliance(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<DeliveryCompliance | null> {
    const result = await this.service.getCompliance(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "createdBy",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getCreatedBy(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<User | null> {
    const result = await this.service.getCreatedBy(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Customer, {
    nullable: true,
    name: "customer",
  })
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async getCustomer(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<Customer | null> {
    const result = await this.service.getCustomer(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DisputeCase, {
    nullable: true,
    name: "dispute",
  })
  @nestAccessControl.UseRoles({
    resource: "DisputeCase",
    action: "read",
    possession: "any",
  })
  async getDispute(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<DisputeCase | null> {
    const result = await this.service.getDispute(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Payment, {
    nullable: true,
    name: "payment",
  })
  @nestAccessControl.UseRoles({
    resource: "Payment",
    action: "read",
    possession: "any",
  })
  async getPayment(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<Payment | null> {
    const result = await this.service.getPayment(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DriverPayout, {
    nullable: true,
    name: "payout",
  })
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "read",
    possession: "any",
  })
  async getPayout(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<DriverPayout | null> {
    const result = await this.service.getPayout(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Quote, {
    nullable: true,
    name: "quote",
  })
  @nestAccessControl.UseRoles({
    resource: "Quote",
    action: "read",
    possession: "any",
  })
  async getQuote(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<Quote | null> {
    const result = await this.service.getQuote(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRating, {
    nullable: true,
    name: "rating",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "read",
    possession: "any",
  })
  async getRating(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<DeliveryRating | null> {
    const result = await this.service.getRating(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "resubmittedFrom",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getResubmittedFrom(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getResubmittedFrom(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Tip, {
    nullable: true,
    name: "tip",
  })
  @nestAccessControl.UseRoles({
    resource: "Tip",
    action: "read",
    possession: "any",
  })
  async getTip(@graphql.Parent() parent: DeliveryRequest): Promise<Tip | null> {
    const result = await this.service.getTip(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => TrackingSession, {
    nullable: true,
    name: "trackingSession",
  })
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "read",
    possession: "any",
  })
  async getTrackingSession(
    @graphql.Parent() parent: DeliveryRequest
  ): Promise<TrackingSession | null> {
    const result = await this.service.getTrackingSession(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
