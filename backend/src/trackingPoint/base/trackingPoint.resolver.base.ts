/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { TrackingPoint } from "./TrackingPoint";
import { TrackingPointCountArgs } from "./TrackingPointCountArgs";
import { TrackingPointFindManyArgs } from "./TrackingPointFindManyArgs";
import { TrackingPointFindUniqueArgs } from "./TrackingPointFindUniqueArgs";
import { CreateTrackingPointArgs } from "./CreateTrackingPointArgs";
import { UpdateTrackingPointArgs } from "./UpdateTrackingPointArgs";
import { DeleteTrackingPointArgs } from "./DeleteTrackingPointArgs";
import { TrackingSession } from "../../trackingSession/base/TrackingSession";
import { TrackingPointService } from "../trackingPoint.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => TrackingPoint)
export class TrackingPointResolverBase {
  constructor(
    protected readonly service: TrackingPointService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "read",
    possession: "any",
  })
  async _trackingPointsMeta(
    @graphql.Args() args: TrackingPointCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [TrackingPoint])
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "read",
    possession: "any",
  })
  async trackingPoints(
    @graphql.Args() args: TrackingPointFindManyArgs
  ): Promise<TrackingPoint[]> {
    return this.service.trackingPoints(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => TrackingPoint, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "read",
    possession: "own",
  })
  async trackingPoint(
    @graphql.Args() args: TrackingPointFindUniqueArgs
  ): Promise<TrackingPoint | null> {
    const result = await this.service.trackingPoint(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => TrackingPoint)
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "create",
    possession: "any",
  })
  async createTrackingPoint(
    @graphql.Args() args: CreateTrackingPointArgs
  ): Promise<TrackingPoint> {
    return await this.service.createTrackingPoint({
      ...args,
      data: {
        ...args.data,

        session: {
          connect: args.data.session,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => TrackingPoint)
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "update",
    possession: "any",
  })
  async updateTrackingPoint(
    @graphql.Args() args: UpdateTrackingPointArgs
  ): Promise<TrackingPoint | null> {
    try {
      return await this.service.updateTrackingPoint({
        ...args,
        data: {
          ...args.data,

          session: {
            connect: args.data.session,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => TrackingPoint)
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "delete",
    possession: "any",
  })
  async deleteTrackingPoint(
    @graphql.Args() args: DeleteTrackingPointArgs
  ): Promise<TrackingPoint | null> {
    try {
      return await this.service.deleteTrackingPoint(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => TrackingSession, {
    nullable: true,
    name: "session",
  })
  @nestAccessControl.UseRoles({
    resource: "TrackingSession",
    action: "read",
    possession: "any",
  })
  async getSession(
    @graphql.Parent() parent: TrackingPoint
  ): Promise<TrackingSession | null> {
    const result = await this.service.getSession(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
