/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TrackingPointService } from "../trackingPoint.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TrackingPointCreateInput } from "./TrackingPointCreateInput";
import { TrackingPoint } from "./TrackingPoint";
import { TrackingPointFindManyArgs } from "./TrackingPointFindManyArgs";
import { TrackingPointWhereUniqueInput } from "./TrackingPointWhereUniqueInput";
import { TrackingPointUpdateInput } from "./TrackingPointUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TrackingPointControllerBase {
  constructor(
    protected readonly service: TrackingPointService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TrackingPoint })
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createTrackingPoint(
    @common.Body() data: TrackingPointCreateInput
  ): Promise<TrackingPoint> {
    return await this.service.createTrackingPoint({
      data: {
        ...data,

        session: {
          connect: data.session,
        },
      },
      select: {
        createdAt: true,
        id: true,
        lat: true,
        lng: true,
        recordedAt: true,

        session: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TrackingPoint] })
  @ApiNestedQuery(TrackingPointFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async trackingPoints(
    @common.Req() request: Request
  ): Promise<TrackingPoint[]> {
    const args = plainToClass(TrackingPointFindManyArgs, request.query);
    return this.service.trackingPoints({
      ...args,
      select: {
        createdAt: true,
        id: true,
        lat: true,
        lng: true,
        recordedAt: true,

        session: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TrackingPoint })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async trackingPoint(
    @common.Param() params: TrackingPointWhereUniqueInput
  ): Promise<TrackingPoint | null> {
    const result = await this.service.trackingPoint({
      where: params,
      select: {
        createdAt: true,
        id: true,
        lat: true,
        lng: true,
        recordedAt: true,

        session: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TrackingPoint })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateTrackingPoint(
    @common.Param() params: TrackingPointWhereUniqueInput,
    @common.Body() data: TrackingPointUpdateInput
  ): Promise<TrackingPoint | null> {
    try {
      return await this.service.updateTrackingPoint({
        where: params,
        data: {
          ...data,

          session: {
            connect: data.session,
          },
        },
        select: {
          createdAt: true,
          id: true,
          lat: true,
          lng: true,
          recordedAt: true,

          session: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TrackingPoint })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TrackingPoint",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteTrackingPoint(
    @common.Param() params: TrackingPointWhereUniqueInput
  ): Promise<TrackingPoint | null> {
    try {
      return await this.service.deleteTrackingPoint({
        where: params,
        select: {
          createdAt: true,
          id: true,
          lat: true,
          lng: true,
          recordedAt: true,

          session: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
