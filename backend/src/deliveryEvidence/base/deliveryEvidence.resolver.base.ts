/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DeliveryEvidence } from "./DeliveryEvidence";
import { DeliveryEvidenceCountArgs } from "./DeliveryEvidenceCountArgs";
import { DeliveryEvidenceFindManyArgs } from "./DeliveryEvidenceFindManyArgs";
import { DeliveryEvidenceFindUniqueArgs } from "./DeliveryEvidenceFindUniqueArgs";
import { CreateDeliveryEvidenceArgs } from "./CreateDeliveryEvidenceArgs";
import { UpdateDeliveryEvidenceArgs } from "./UpdateDeliveryEvidenceArgs";
import { DeleteDeliveryEvidenceArgs } from "./DeleteDeliveryEvidenceArgs";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { DeliveryEvidenceService } from "../deliveryEvidence.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DeliveryEvidence)
export class DeliveryEvidenceResolverBase {
  constructor(
    protected readonly service: DeliveryEvidenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DeliveryEvidence",
    action: "read",
    possession: "any",
  })
  async _deliveryEvidencesMeta(
    @graphql.Args() args: DeliveryEvidenceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DeliveryEvidence])
  @nestAccessControl.UseRoles({
    resource: "DeliveryEvidence",
    action: "read",
    possession: "any",
  })
  async deliveryEvidences(
    @graphql.Args() args: DeliveryEvidenceFindManyArgs
  ): Promise<DeliveryEvidence[]> {
    return this.service.deliveryEvidences(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DeliveryEvidence, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DeliveryEvidence",
    action: "read",
    possession: "own",
  })
  async deliveryEvidence(
    @graphql.Args() args: DeliveryEvidenceFindUniqueArgs
  ): Promise<DeliveryEvidence | null> {
    const result = await this.service.deliveryEvidence(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryEvidence)
  @nestAccessControl.UseRoles({
    resource: "DeliveryEvidence",
    action: "create",
    possession: "any",
  })
  async createDeliveryEvidence(
    @graphql.Args() args: CreateDeliveryEvidenceArgs
  ): Promise<DeliveryEvidence> {
    return await this.service.createDeliveryEvidence({
      ...args,
      data: {
        ...args.data,

        delivery: {
          connect: args.data.delivery,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryEvidence)
  @nestAccessControl.UseRoles({
    resource: "DeliveryEvidence",
    action: "update",
    possession: "any",
  })
  async updateDeliveryEvidence(
    @graphql.Args() args: UpdateDeliveryEvidenceArgs
  ): Promise<DeliveryEvidence | null> {
    try {
      return await this.service.updateDeliveryEvidence({
        ...args,
        data: {
          ...args.data,

          delivery: {
            connect: args.data.delivery,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DeliveryEvidence)
  @nestAccessControl.UseRoles({
    resource: "DeliveryEvidence",
    action: "delete",
    possession: "any",
  })
  async deleteDeliveryEvidence(
    @graphql.Args() args: DeleteDeliveryEvidenceArgs
  ): Promise<DeliveryEvidence | null> {
    try {
      return await this.service.deleteDeliveryEvidence(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "delivery",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getDelivery(
    @graphql.Parent() parent: DeliveryEvidence
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getDelivery(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
