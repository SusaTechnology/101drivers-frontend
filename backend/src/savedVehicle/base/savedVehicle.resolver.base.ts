/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { SavedVehicle } from "./SavedVehicle";
import { SavedVehicleCountArgs } from "./SavedVehicleCountArgs";
import { SavedVehicleFindManyArgs } from "./SavedVehicleFindManyArgs";
import { SavedVehicleFindUniqueArgs } from "./SavedVehicleFindUniqueArgs";
import { CreateSavedVehicleArgs } from "./CreateSavedVehicleArgs";
import { UpdateSavedVehicleArgs } from "./UpdateSavedVehicleArgs";
import { DeleteSavedVehicleArgs } from "./DeleteSavedVehicleArgs";
import { Customer } from "../../customer/base/Customer";
import { SavedVehicleService } from "../savedVehicle.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SavedVehicle)
export class SavedVehicleResolverBase {
  constructor(
    protected readonly service: SavedVehicleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SavedVehicle",
    action: "read",
    possession: "any",
  })
  async _savedVehiclesMeta(
    @graphql.Args() args: SavedVehicleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SavedVehicle])
  @nestAccessControl.UseRoles({
    resource: "SavedVehicle",
    action: "read",
    possession: "any",
  })
  async savedVehicles(
    @graphql.Args() args: SavedVehicleFindManyArgs
  ): Promise<SavedVehicle[]> {
    return this.service.savedVehicles(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SavedVehicle, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SavedVehicle",
    action: "read",
    possession: "own",
  })
  async savedVehicle(
    @graphql.Args() args: SavedVehicleFindUniqueArgs
  ): Promise<SavedVehicle | null> {
    const result = await this.service.savedVehicle(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SavedVehicle)
  @nestAccessControl.UseRoles({
    resource: "SavedVehicle",
    action: "create",
    possession: "any",
  })
  async createSavedVehicle(
    @graphql.Args() args: CreateSavedVehicleArgs
  ): Promise<SavedVehicle> {
    return await this.service.createSavedVehicle({
      ...args,
      data: {
        ...args.data,

        customer: {
          connect: args.data.customer,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SavedVehicle)
  @nestAccessControl.UseRoles({
    resource: "SavedVehicle",
    action: "update",
    possession: "any",
  })
  async updateSavedVehicle(
    @graphql.Args() args: UpdateSavedVehicleArgs
  ): Promise<SavedVehicle | null> {
    try {
      return await this.service.updateSavedVehicle({
        ...args,
        data: {
          ...args.data,

          customer: {
            connect: args.data.customer,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => SavedVehicle)
  @nestAccessControl.UseRoles({
    resource: "SavedVehicle",
    action: "delete",
    possession: "any",
  })
  async deleteSavedVehicle(
    @graphql.Args() args: DeleteSavedVehicleArgs
  ): Promise<SavedVehicle | null> {
    try {
      return await this.service.deleteSavedVehicle(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Customer, {
    nullable: true,
    name: "customer",
  })
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async getCustomer(
    @graphql.Parent() parent: SavedVehicle
  ): Promise<Customer | null> {
    const result = await this.service.getCustomer(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
