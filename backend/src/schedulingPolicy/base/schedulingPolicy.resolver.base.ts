/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { SchedulingPolicy } from "./SchedulingPolicy";
import { SchedulingPolicyCountArgs } from "./SchedulingPolicyCountArgs";
import { SchedulingPolicyFindManyArgs } from "./SchedulingPolicyFindManyArgs";
import { SchedulingPolicyFindUniqueArgs } from "./SchedulingPolicyFindUniqueArgs";
import { CreateSchedulingPolicyArgs } from "./CreateSchedulingPolicyArgs";
import { UpdateSchedulingPolicyArgs } from "./UpdateSchedulingPolicyArgs";
import { DeleteSchedulingPolicyArgs } from "./DeleteSchedulingPolicyArgs";
import { SchedulingPolicyService } from "../schedulingPolicy.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SchedulingPolicy)
export class SchedulingPolicyResolverBase {
  constructor(
    protected readonly service: SchedulingPolicyService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SchedulingPolicy",
    action: "read",
    possession: "any",
  })
  async _schedulingPoliciesMeta(
    @graphql.Args() args: SchedulingPolicyCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SchedulingPolicy])
  @nestAccessControl.UseRoles({
    resource: "SchedulingPolicy",
    action: "read",
    possession: "any",
  })
  async schedulingPolicies(
    @graphql.Args() args: SchedulingPolicyFindManyArgs
  ): Promise<SchedulingPolicy[]> {
    return this.service.schedulingPolicies(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SchedulingPolicy, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SchedulingPolicy",
    action: "read",
    possession: "own",
  })
  async schedulingPolicy(
    @graphql.Args() args: SchedulingPolicyFindUniqueArgs
  ): Promise<SchedulingPolicy | null> {
    const result = await this.service.schedulingPolicy(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SchedulingPolicy)
  @nestAccessControl.UseRoles({
    resource: "SchedulingPolicy",
    action: "create",
    possession: "any",
  })
  async createSchedulingPolicy(
    @graphql.Args() args: CreateSchedulingPolicyArgs
  ): Promise<SchedulingPolicy> {
    return await this.service.createSchedulingPolicy({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SchedulingPolicy)
  @nestAccessControl.UseRoles({
    resource: "SchedulingPolicy",
    action: "update",
    possession: "any",
  })
  async updateSchedulingPolicy(
    @graphql.Args() args: UpdateSchedulingPolicyArgs
  ): Promise<SchedulingPolicy | null> {
    try {
      return await this.service.updateSchedulingPolicy({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => SchedulingPolicy)
  @nestAccessControl.UseRoles({
    resource: "SchedulingPolicy",
    action: "delete",
    possession: "any",
  })
  async deleteSchedulingPolicy(
    @graphql.Args() args: DeleteSchedulingPolicyArgs
  ): Promise<SchedulingPolicy | null> {
    try {
      return await this.service.deleteSchedulingPolicy(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
