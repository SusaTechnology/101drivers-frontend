/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { SavedAddress } from "./SavedAddress";
import { SavedAddressCountArgs } from "./SavedAddressCountArgs";
import { SavedAddressFindManyArgs } from "./SavedAddressFindManyArgs";
import { SavedAddressFindUniqueArgs } from "./SavedAddressFindUniqueArgs";
import { CreateSavedAddressArgs } from "./CreateSavedAddressArgs";
import { UpdateSavedAddressArgs } from "./UpdateSavedAddressArgs";
import { DeleteSavedAddressArgs } from "./DeleteSavedAddressArgs";
import { CustomerFindManyArgs } from "../../customer/base/CustomerFindManyArgs";
import { Customer } from "../../customer/base/Customer";
import { SavedAddressService } from "../savedAddress.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SavedAddress)
export class SavedAddressResolverBase {
  constructor(
    protected readonly service: SavedAddressService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "read",
    possession: "any",
  })
  async _savedAddressesMeta(
    @graphql.Args() args: SavedAddressCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SavedAddress])
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "read",
    possession: "any",
  })
  async savedAddresses(
    @graphql.Args() args: SavedAddressFindManyArgs
  ): Promise<SavedAddress[]> {
    return this.service.savedAddresses(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SavedAddress, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "read",
    possession: "own",
  })
  async savedAddress(
    @graphql.Args() args: SavedAddressFindUniqueArgs
  ): Promise<SavedAddress | null> {
    const result = await this.service.savedAddress(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SavedAddress)
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "create",
    possession: "any",
  })
  async createSavedAddress(
    @graphql.Args() args: CreateSavedAddressArgs
  ): Promise<SavedAddress> {
    return await this.service.createSavedAddress({
      ...args,
      data: {
        ...args.data,

        customer: {
          connect: args.data.customer,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => SavedAddress)
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "update",
    possession: "any",
  })
  async updateSavedAddress(
    @graphql.Args() args: UpdateSavedAddressArgs
  ): Promise<SavedAddress | null> {
    try {
      return await this.service.updateSavedAddress({
        ...args,
        data: {
          ...args.data,

          customer: {
            connect: args.data.customer,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => SavedAddress)
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "delete",
    possession: "any",
  })
  async deleteSavedAddress(
    @graphql.Args() args: DeleteSavedAddressArgs
  ): Promise<SavedAddress | null> {
    try {
      return await this.service.deleteSavedAddress(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [Customer], { name: "defaultForCustomers" })
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async findDefaultForCustomers(
    @graphql.Parent() parent: SavedAddress,
    @graphql.Args() args: CustomerFindManyArgs
  ): Promise<Customer[]> {
    const results = await this.service.findDefaultForCustomers(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Customer, {
    nullable: true,
    name: "customer",
  })
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async getCustomer(
    @graphql.Parent() parent: SavedAddress
  ): Promise<Customer | null> {
    const result = await this.service.getCustomer(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
