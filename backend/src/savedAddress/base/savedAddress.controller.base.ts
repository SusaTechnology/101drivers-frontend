/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { SavedAddressService } from "../savedAddress.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SavedAddressCreateInput } from "./SavedAddressCreateInput";
import { SavedAddress } from "./SavedAddress";
import { SavedAddressFindManyArgs } from "./SavedAddressFindManyArgs";
import { SavedAddressWhereUniqueInput } from "./SavedAddressWhereUniqueInput";
import { SavedAddressUpdateInput } from "./SavedAddressUpdateInput";
import { CustomerFindManyArgs } from "../../customer/base/CustomerFindManyArgs";
import { Customer } from "../../customer/base/Customer";
import { CustomerWhereUniqueInput } from "../../customer/base/CustomerWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SavedAddressControllerBase {
  constructor(
    protected readonly service: SavedAddressService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SavedAddress })
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createSavedAddress(
    @common.Body() data: SavedAddressCreateInput
  ): Promise<SavedAddress> {
    return await this.service.createSavedAddress({
      data: {
        ...data,

        customer: {
          connect: data.customer,
        },
      },
      select: {
        address: true,
        city: true,
        country: true,
        createdAt: true,

        customer: {
          select: {
            id: true,
          },
        },

        id: true,
        isDefault: true,
        label: true,
        lat: true,
        lng: true,
        placeId: true,
        postalCode: true,
        state: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [SavedAddress] })
  @ApiNestedQuery(SavedAddressFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async savedAddresses(
    @common.Req() request: Request
  ): Promise<SavedAddress[]> {
    const args = plainToClass(SavedAddressFindManyArgs, request.query);
    return this.service.savedAddresses({
      ...args,
      select: {
        address: true,
        city: true,
        country: true,
        createdAt: true,

        customer: {
          select: {
            id: true,
          },
        },

        id: true,
        isDefault: true,
        label: true,
        lat: true,
        lng: true,
        placeId: true,
        postalCode: true,
        state: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SavedAddress })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async savedAddress(
    @common.Param() params: SavedAddressWhereUniqueInput
  ): Promise<SavedAddress | null> {
    const result = await this.service.savedAddress({
      where: params,
      select: {
        address: true,
        city: true,
        country: true,
        createdAt: true,

        customer: {
          select: {
            id: true,
          },
        },

        id: true,
        isDefault: true,
        label: true,
        lat: true,
        lng: true,
        placeId: true,
        postalCode: true,
        state: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SavedAddress })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateSavedAddress(
    @common.Param() params: SavedAddressWhereUniqueInput,
    @common.Body() data: SavedAddressUpdateInput
  ): Promise<SavedAddress | null> {
    try {
      return await this.service.updateSavedAddress({
        where: params,
        data: {
          ...data,

          customer: {
            connect: data.customer,
          },
        },
        select: {
          address: true,
          city: true,
          country: true,
          createdAt: true,

          customer: {
            select: {
              id: true,
            },
          },

          id: true,
          isDefault: true,
          label: true,
          lat: true,
          lng: true,
          placeId: true,
          postalCode: true,
          state: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SavedAddress })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteSavedAddress(
    @common.Param() params: SavedAddressWhereUniqueInput
  ): Promise<SavedAddress | null> {
    try {
      return await this.service.deleteSavedAddress({
        where: params,
        select: {
          address: true,
          city: true,
          country: true,
          createdAt: true,

          customer: {
            select: {
              id: true,
            },
          },

          id: true,
          isDefault: true,
          label: true,
          lat: true,
          lng: true,
          placeId: true,
          postalCode: true,
          state: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/defaultForCustomers")
  @ApiNestedQuery(CustomerFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async findDefaultForCustomers(
    @common.Req() request: Request,
    @common.Param() params: SavedAddressWhereUniqueInput
  ): Promise<Customer[]> {
    const query = plainToClass(CustomerFindManyArgs, request.query);
    const results = await this.service.findDefaultForCustomers(params.id, {
      ...query,
      select: {
        approvalStatus: true,
        approvedAt: true,

        approvedBy: {
          select: {
            id: true,
          },
        },

        businessAddress: true,
        businessName: true,
        businessPhone: true,
        businessPlaceId: true,
        businessWebsite: true,
        contactEmail: true,
        contactName: true,
        contactPhone: true,
        createdAt: true,
        customerType: true,

        defaultPickup: {
          select: {
            id: true,
          },
        },

        id: true,
        phone: true,
        postpaidEnabled: true,

        pricingConfig: {
          select: {
            id: true,
          },
        },

        pricingModeOverride: true,
        suspendedAt: true,
        suspensionReason: true,
        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/defaultForCustomers")
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "update",
    possession: "any",
  })
  async connectDefaultForCustomers(
    @common.Param() params: SavedAddressWhereUniqueInput,
    @common.Body() body: CustomerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      defaultForCustomers: {
        connect: body,
      },
    };
    await this.service.updateSavedAddress({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/defaultForCustomers")
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "update",
    possession: "any",
  })
  async updateDefaultForCustomers(
    @common.Param() params: SavedAddressWhereUniqueInput,
    @common.Body() body: CustomerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      defaultForCustomers: {
        set: body,
      },
    };
    await this.service.updateSavedAddress({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/defaultForCustomers")
  @nestAccessControl.UseRoles({
    resource: "SavedAddress",
    action: "update",
    possession: "any",
  })
  async disconnectDefaultForCustomers(
    @common.Param() params: SavedAddressWhereUniqueInput,
    @common.Body() body: CustomerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      defaultForCustomers: {
        disconnect: body,
      },
    };
    await this.service.updateSavedAddress({
      where: params,
      data,
      select: { id: true },
    });
  }
}
