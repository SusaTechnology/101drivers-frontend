/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { PaymentEvent } from "./PaymentEvent";
import { PaymentEventCountArgs } from "./PaymentEventCountArgs";
import { PaymentEventFindManyArgs } from "./PaymentEventFindManyArgs";
import { PaymentEventFindUniqueArgs } from "./PaymentEventFindUniqueArgs";
import { CreatePaymentEventArgs } from "./CreatePaymentEventArgs";
import { UpdatePaymentEventArgs } from "./UpdatePaymentEventArgs";
import { DeletePaymentEventArgs } from "./DeletePaymentEventArgs";
import { Payment } from "../../payment/base/Payment";
import { PaymentEventService } from "../paymentEvent.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PaymentEvent)
export class PaymentEventResolverBase {
  constructor(
    protected readonly service: PaymentEventService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "read",
    possession: "any",
  })
  async _paymentEventsMeta(
    @graphql.Args() args: PaymentEventCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PaymentEvent])
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "read",
    possession: "any",
  })
  async paymentEvents(
    @graphql.Args() args: PaymentEventFindManyArgs
  ): Promise<PaymentEvent[]> {
    return this.service.paymentEvents(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PaymentEvent, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "read",
    possession: "own",
  })
  async paymentEvent(
    @graphql.Args() args: PaymentEventFindUniqueArgs
  ): Promise<PaymentEvent | null> {
    const result = await this.service.paymentEvent(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => PaymentEvent)
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "create",
    possession: "any",
  })
  async createPaymentEvent(
    @graphql.Args() args: CreatePaymentEventArgs
  ): Promise<PaymentEvent> {
    return await this.service.createPaymentEvent({
      ...args,
      data: {
        ...args.data,

        payment: {
          connect: args.data.payment,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => PaymentEvent)
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "update",
    possession: "any",
  })
  async updatePaymentEvent(
    @graphql.Args() args: UpdatePaymentEventArgs
  ): Promise<PaymentEvent | null> {
    try {
      return await this.service.updatePaymentEvent({
        ...args,
        data: {
          ...args.data,

          payment: {
            connect: args.data.payment,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => PaymentEvent)
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "delete",
    possession: "any",
  })
  async deletePaymentEvent(
    @graphql.Args() args: DeletePaymentEventArgs
  ): Promise<PaymentEvent | null> {
    try {
      return await this.service.deletePaymentEvent(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Payment, {
    nullable: true,
    name: "payment",
  })
  @nestAccessControl.UseRoles({
    resource: "Payment",
    action: "read",
    possession: "any",
  })
  async getPayment(
    @graphql.Parent() parent: PaymentEvent
  ): Promise<Payment | null> {
    const result = await this.service.getPayment(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
