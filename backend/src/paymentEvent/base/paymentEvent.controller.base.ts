/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PaymentEventService } from "../paymentEvent.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PaymentEventCreateInput } from "./PaymentEventCreateInput";
import { PaymentEvent } from "./PaymentEvent";
import { PaymentEventFindManyArgs } from "./PaymentEventFindManyArgs";
import { PaymentEventWhereUniqueInput } from "./PaymentEventWhereUniqueInput";
import { PaymentEventUpdateInput } from "./PaymentEventUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PaymentEventControllerBase {
  constructor(
    protected readonly service: PaymentEventService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PaymentEvent })
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPaymentEvent(
    @common.Body() data: PaymentEventCreateInput
  ): Promise<PaymentEvent> {
    return await this.service.createPaymentEvent({
      data: {
        ...data,

        payment: {
          connect: data.payment,
        },
      },
      select: {
        amount: true,
        createdAt: true,
        id: true,
        message: true,

        payment: {
          select: {
            id: true,
          },
        },

        providerRef: true,
        raw: true,
        status: true,
        type: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PaymentEvent] })
  @ApiNestedQuery(PaymentEventFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paymentEvents(@common.Req() request: Request): Promise<PaymentEvent[]> {
    const args = plainToClass(PaymentEventFindManyArgs, request.query);
    return this.service.paymentEvents({
      ...args,
      select: {
        amount: true,
        createdAt: true,
        id: true,
        message: true,

        payment: {
          select: {
            id: true,
          },
        },

        providerRef: true,
        raw: true,
        status: true,
        type: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PaymentEvent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paymentEvent(
    @common.Param() params: PaymentEventWhereUniqueInput
  ): Promise<PaymentEvent | null> {
    const result = await this.service.paymentEvent({
      where: params,
      select: {
        amount: true,
        createdAt: true,
        id: true,
        message: true,

        payment: {
          select: {
            id: true,
          },
        },

        providerRef: true,
        raw: true,
        status: true,
        type: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PaymentEvent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePaymentEvent(
    @common.Param() params: PaymentEventWhereUniqueInput,
    @common.Body() data: PaymentEventUpdateInput
  ): Promise<PaymentEvent | null> {
    try {
      return await this.service.updatePaymentEvent({
        where: params,
        data: {
          ...data,

          payment: {
            connect: data.payment,
          },
        },
        select: {
          amount: true,
          createdAt: true,
          id: true,
          message: true,

          payment: {
            select: {
              id: true,
            },
          },

          providerRef: true,
          raw: true,
          status: true,
          type: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PaymentEvent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentEvent",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePaymentEvent(
    @common.Param() params: PaymentEventWhereUniqueInput
  ): Promise<PaymentEvent | null> {
    try {
      return await this.service.deletePaymentEvent({
        where: params,
        select: {
          amount: true,
          createdAt: true,
          id: true,
          message: true,

          payment: {
            select: {
              id: true,
            },
          },

          providerRef: true,
          raw: true,
          status: true,
          type: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
