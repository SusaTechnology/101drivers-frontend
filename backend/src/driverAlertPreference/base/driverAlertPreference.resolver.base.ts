/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DriverAlertPreference } from "./DriverAlertPreference";
import { DriverAlertPreferenceCountArgs } from "./DriverAlertPreferenceCountArgs";
import { DriverAlertPreferenceFindManyArgs } from "./DriverAlertPreferenceFindManyArgs";
import { DriverAlertPreferenceFindUniqueArgs } from "./DriverAlertPreferenceFindUniqueArgs";
import { CreateDriverAlertPreferenceArgs } from "./CreateDriverAlertPreferenceArgs";
import { UpdateDriverAlertPreferenceArgs } from "./UpdateDriverAlertPreferenceArgs";
import { DeleteDriverAlertPreferenceArgs } from "./DeleteDriverAlertPreferenceArgs";
import { Driver } from "../../driver/base/Driver";
import { DriverAlertPreferenceService } from "../driverAlertPreference.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DriverAlertPreference)
export class DriverAlertPreferenceResolverBase {
  constructor(
    protected readonly service: DriverAlertPreferenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "read",
    possession: "any",
  })
  async _driverAlertPreferencesMeta(
    @graphql.Args() args: DriverAlertPreferenceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DriverAlertPreference])
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "read",
    possession: "any",
  })
  async driverAlertPreferences(
    @graphql.Args() args: DriverAlertPreferenceFindManyArgs
  ): Promise<DriverAlertPreference[]> {
    return this.service.driverAlertPreferences(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DriverAlertPreference, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "read",
    possession: "own",
  })
  async driverAlertPreference(
    @graphql.Args() args: DriverAlertPreferenceFindUniqueArgs
  ): Promise<DriverAlertPreference | null> {
    const result = await this.service.driverAlertPreference(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverAlertPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "create",
    possession: "any",
  })
  async createDriverAlertPreference(
    @graphql.Args() args: CreateDriverAlertPreferenceArgs
  ): Promise<DriverAlertPreference> {
    return await this.service.createDriverAlertPreference({
      ...args,
      data: {
        ...args.data,

        driver: {
          connect: args.data.driver,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverAlertPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "update",
    possession: "any",
  })
  async updateDriverAlertPreference(
    @graphql.Args() args: UpdateDriverAlertPreferenceArgs
  ): Promise<DriverAlertPreference | null> {
    try {
      return await this.service.updateDriverAlertPreference({
        ...args,
        data: {
          ...args.data,

          driver: {
            connect: args.data.driver,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DriverAlertPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "delete",
    possession: "any",
  })
  async deleteDriverAlertPreference(
    @graphql.Args() args: DeleteDriverAlertPreferenceArgs
  ): Promise<DriverAlertPreference | null> {
    try {
      return await this.service.deleteDriverAlertPreference(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Driver, {
    nullable: true,
    name: "driver",
  })
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async getDriver(
    @graphql.Parent() parent: DriverAlertPreference
  ): Promise<Driver | null> {
    const result = await this.service.getDriver(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
