/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DriverAlertPreferenceService } from "../driverAlertPreference.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DriverAlertPreferenceCreateInput } from "./DriverAlertPreferenceCreateInput";
import { DriverAlertPreference } from "./DriverAlertPreference";
import { DriverAlertPreferenceFindManyArgs } from "./DriverAlertPreferenceFindManyArgs";
import { DriverAlertPreferenceWhereUniqueInput } from "./DriverAlertPreferenceWhereUniqueInput";
import { DriverAlertPreferenceUpdateInput } from "./DriverAlertPreferenceUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DriverAlertPreferenceControllerBase {
  constructor(
    protected readonly service: DriverAlertPreferenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DriverAlertPreference })
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDriverAlertPreference(
    @common.Body() data: DriverAlertPreferenceCreateInput
  ): Promise<DriverAlertPreference> {
    return await this.service.createDriverAlertPreference({
      data: {
        ...data,

        driver: {
          connect: data.driver,
        },
      },
      select: {
        createdAt: true,

        driver: {
          select: {
            id: true,
          },
        },

        emailEnabled: true,
        enabled: true,
        id: true,
        smsEnabled: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DriverAlertPreference] })
  @ApiNestedQuery(DriverAlertPreferenceFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async driverAlertPreferences(
    @common.Req() request: Request
  ): Promise<DriverAlertPreference[]> {
    const args = plainToClass(DriverAlertPreferenceFindManyArgs, request.query);
    return this.service.driverAlertPreferences({
      ...args,
      select: {
        createdAt: true,

        driver: {
          select: {
            id: true,
          },
        },

        emailEnabled: true,
        enabled: true,
        id: true,
        smsEnabled: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DriverAlertPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async driverAlertPreference(
    @common.Param() params: DriverAlertPreferenceWhereUniqueInput
  ): Promise<DriverAlertPreference | null> {
    const result = await this.service.driverAlertPreference({
      where: params,
      select: {
        createdAt: true,

        driver: {
          select: {
            id: true,
          },
        },

        emailEnabled: true,
        enabled: true,
        id: true,
        smsEnabled: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DriverAlertPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDriverAlertPreference(
    @common.Param() params: DriverAlertPreferenceWhereUniqueInput,
    @common.Body() data: DriverAlertPreferenceUpdateInput
  ): Promise<DriverAlertPreference | null> {
    try {
      return await this.service.updateDriverAlertPreference({
        where: params,
        data: {
          ...data,

          driver: {
            connect: data.driver,
          },
        },
        select: {
          createdAt: true,

          driver: {
            select: {
              id: true,
            },
          },

          emailEnabled: true,
          enabled: true,
          id: true,
          smsEnabled: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DriverAlertPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDriverAlertPreference(
    @common.Param() params: DriverAlertPreferenceWhereUniqueInput
  ): Promise<DriverAlertPreference | null> {
    try {
      return await this.service.deleteDriverAlertPreference({
        where: params,
        select: {
          createdAt: true,

          driver: {
            select: {
              id: true,
            },
          },

          emailEnabled: true,
          enabled: true,
          id: true,
          smsEnabled: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
