/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DriverPreference } from "./DriverPreference";
import { DriverPreferenceCountArgs } from "./DriverPreferenceCountArgs";
import { DriverPreferenceFindManyArgs } from "./DriverPreferenceFindManyArgs";
import { DriverPreferenceFindUniqueArgs } from "./DriverPreferenceFindUniqueArgs";
import { CreateDriverPreferenceArgs } from "./CreateDriverPreferenceArgs";
import { UpdateDriverPreferenceArgs } from "./UpdateDriverPreferenceArgs";
import { DeleteDriverPreferenceArgs } from "./DeleteDriverPreferenceArgs";
import { Driver } from "../../driver/base/Driver";
import { DriverPreferenceService } from "../driverPreference.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DriverPreference)
export class DriverPreferenceResolverBase {
  constructor(
    protected readonly service: DriverPreferenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DriverPreference",
    action: "read",
    possession: "any",
  })
  async _driverPreferencesMeta(
    @graphql.Args() args: DriverPreferenceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DriverPreference])
  @nestAccessControl.UseRoles({
    resource: "DriverPreference",
    action: "read",
    possession: "any",
  })
  async driverPreferences(
    @graphql.Args() args: DriverPreferenceFindManyArgs
  ): Promise<DriverPreference[]> {
    return this.service.driverPreferences(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DriverPreference, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DriverPreference",
    action: "read",
    possession: "own",
  })
  async driverPreference(
    @graphql.Args() args: DriverPreferenceFindUniqueArgs
  ): Promise<DriverPreference | null> {
    const result = await this.service.driverPreference(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverPreference",
    action: "create",
    possession: "any",
  })
  async createDriverPreference(
    @graphql.Args() args: CreateDriverPreferenceArgs
  ): Promise<DriverPreference> {
    return await this.service.createDriverPreference({
      ...args,
      data: {
        ...args.data,

        driver: {
          connect: args.data.driver,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DriverPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverPreference",
    action: "update",
    possession: "any",
  })
  async updateDriverPreference(
    @graphql.Args() args: UpdateDriverPreferenceArgs
  ): Promise<DriverPreference | null> {
    try {
      return await this.service.updateDriverPreference({
        ...args,
        data: {
          ...args.data,

          driver: {
            connect: args.data.driver,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DriverPreference)
  @nestAccessControl.UseRoles({
    resource: "DriverPreference",
    action: "delete",
    possession: "any",
  })
  async deleteDriverPreference(
    @graphql.Args() args: DeleteDriverPreferenceArgs
  ): Promise<DriverPreference | null> {
    try {
      return await this.service.deleteDriverPreference(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Driver, {
    nullable: true,
    name: "driver",
  })
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async getDriver(
    @graphql.Parent() parent: DriverPreference
  ): Promise<Driver | null> {
    const result = await this.service.getDriver(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
