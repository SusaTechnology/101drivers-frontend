/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DeliveryRating } from "./DeliveryRating";
import { DeliveryRatingCountArgs } from "./DeliveryRatingCountArgs";
import { DeliveryRatingFindManyArgs } from "./DeliveryRatingFindManyArgs";
import { DeliveryRatingFindUniqueArgs } from "./DeliveryRatingFindUniqueArgs";
import { CreateDeliveryRatingArgs } from "./CreateDeliveryRatingArgs";
import { UpdateDeliveryRatingArgs } from "./UpdateDeliveryRatingArgs";
import { DeleteDeliveryRatingArgs } from "./DeleteDeliveryRatingArgs";
import { Customer } from "../../customer/base/Customer";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { Driver } from "../../driver/base/Driver";
import { DeliveryRatingService } from "../deliveryRating.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DeliveryRating)
export class DeliveryRatingResolverBase {
  constructor(
    protected readonly service: DeliveryRatingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "read",
    possession: "any",
  })
  async _deliveryRatingsMeta(
    @graphql.Args() args: DeliveryRatingCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DeliveryRating])
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "read",
    possession: "any",
  })
  async deliveryRatings(
    @graphql.Args() args: DeliveryRatingFindManyArgs
  ): Promise<DeliveryRating[]> {
    return this.service.deliveryRatings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DeliveryRating, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "read",
    possession: "own",
  })
  async deliveryRating(
    @graphql.Args() args: DeliveryRatingFindUniqueArgs
  ): Promise<DeliveryRating | null> {
    const result = await this.service.deliveryRating(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryRating)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "create",
    possession: "any",
  })
  async createDeliveryRating(
    @graphql.Args() args: CreateDeliveryRatingArgs
  ): Promise<DeliveryRating> {
    return await this.service.createDeliveryRating({
      ...args,
      data: {
        ...args.data,

        customer: args.data.customer
          ? {
              connect: args.data.customer,
            }
          : undefined,

        delivery: {
          connect: args.data.delivery,
        },

        driver: args.data.driver
          ? {
              connect: args.data.driver,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DeliveryRating)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "update",
    possession: "any",
  })
  async updateDeliveryRating(
    @graphql.Args() args: UpdateDeliveryRatingArgs
  ): Promise<DeliveryRating | null> {
    try {
      return await this.service.updateDeliveryRating({
        ...args,
        data: {
          ...args.data,

          customer: args.data.customer
            ? {
                connect: args.data.customer,
              }
            : undefined,

          delivery: {
            connect: args.data.delivery,
          },

          driver: args.data.driver
            ? {
                connect: args.data.driver,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DeliveryRating)
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "delete",
    possession: "any",
  })
  async deleteDeliveryRating(
    @graphql.Args() args: DeleteDeliveryRatingArgs
  ): Promise<DeliveryRating | null> {
    try {
      return await this.service.deleteDeliveryRating(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Customer, {
    nullable: true,
    name: "customer",
  })
  @nestAccessControl.UseRoles({
    resource: "Customer",
    action: "read",
    possession: "any",
  })
  async getCustomer(
    @graphql.Parent() parent: DeliveryRating
  ): Promise<Customer | null> {
    const result = await this.service.getCustomer(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "delivery",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getDelivery(
    @graphql.Parent() parent: DeliveryRating
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getDelivery(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Driver, {
    nullable: true,
    name: "driver",
  })
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async getDriver(
    @graphql.Parent() parent: DeliveryRating
  ): Promise<Driver | null> {
    const result = await this.service.getDriver(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
