/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { Driver } from "./Driver";
import { DriverCountArgs } from "./DriverCountArgs";
import { DriverFindManyArgs } from "./DriverFindManyArgs";
import { DriverFindUniqueArgs } from "./DriverFindUniqueArgs";
import { CreateDriverArgs } from "./CreateDriverArgs";
import { UpdateDriverArgs } from "./UpdateDriverArgs";
import { DeleteDriverArgs } from "./DeleteDriverArgs";
import { DeliveryAssignmentFindManyArgs } from "../../deliveryAssignment/base/DeliveryAssignmentFindManyArgs";
import { DeliveryAssignment } from "../../deliveryAssignment/base/DeliveryAssignment";
import { AdminAuditLogFindManyArgs } from "../../adminAuditLog/base/AdminAuditLogFindManyArgs";
import { AdminAuditLog } from "../../adminAuditLog/base/AdminAuditLog";
import { DriverDistrictPreferenceFindManyArgs } from "../../driverDistrictPreference/base/DriverDistrictPreferenceFindManyArgs";
import { DriverDistrictPreference } from "../../driverDistrictPreference/base/DriverDistrictPreference";
import { NotificationEventFindManyArgs } from "../../notificationEvent/base/NotificationEventFindManyArgs";
import { NotificationEvent } from "../../notificationEvent/base/NotificationEvent";
import { DriverPayoutFindManyArgs } from "../../driverPayout/base/DriverPayoutFindManyArgs";
import { DriverPayout } from "../../driverPayout/base/DriverPayout";
import { DeliveryRatingFindManyArgs } from "../../deliveryRating/base/DeliveryRatingFindManyArgs";
import { DeliveryRating } from "../../deliveryRating/base/DeliveryRating";
import { DriverAlertPreference } from "../../driverAlertPreference/base/DriverAlertPreference";
import { User } from "../../user/base/User";
import { DriverLocation } from "../../driverLocation/base/DriverLocation";
import { DriverPreference } from "../../driverPreference/base/DriverPreference";
import { DriverService } from "../driver.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Driver)
export class DriverResolverBase {
  constructor(
    protected readonly service: DriverService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async _driversMeta(
    @graphql.Args() args: DriverCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Driver])
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "any",
  })
  async drivers(@graphql.Args() args: DriverFindManyArgs): Promise<Driver[]> {
    return this.service.drivers(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Driver, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "read",
    possession: "own",
  })
  async driver(
    @graphql.Args() args: DriverFindUniqueArgs
  ): Promise<Driver | null> {
    const result = await this.service.driver(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Driver)
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "create",
    possession: "any",
  })
  async createDriver(@graphql.Args() args: CreateDriverArgs): Promise<Driver> {
    return await this.service.createDriver({
      ...args,
      data: {
        ...args.data,

        alerts: args.data.alerts
          ? {
              connect: args.data.alerts,
            }
          : undefined,

        approvedBy: args.data.approvedBy
          ? {
              connect: args.data.approvedBy,
            }
          : undefined,

        location: args.data.location
          ? {
              connect: args.data.location,
            }
          : undefined,

        preferences: args.data.preferences
          ? {
              connect: args.data.preferences,
            }
          : undefined,

        user: {
          connect: args.data.user,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Driver)
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "update",
    possession: "any",
  })
  async updateDriver(
    @graphql.Args() args: UpdateDriverArgs
  ): Promise<Driver | null> {
    try {
      return await this.service.updateDriver({
        ...args,
        data: {
          ...args.data,

          alerts: args.data.alerts
            ? {
                connect: args.data.alerts,
              }
            : undefined,

          approvedBy: args.data.approvedBy
            ? {
                connect: args.data.approvedBy,
              }
            : undefined,

          location: args.data.location
            ? {
                connect: args.data.location,
              }
            : undefined,

          preferences: args.data.preferences
            ? {
                connect: args.data.preferences,
              }
            : undefined,

          user: {
            connect: args.data.user,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Driver)
  @nestAccessControl.UseRoles({
    resource: "Driver",
    action: "delete",
    possession: "any",
  })
  async deleteDriver(
    @graphql.Args() args: DeleteDriverArgs
  ): Promise<Driver | null> {
    try {
      return await this.service.deleteDriver(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryAssignment], { name: "assignments" })
  @nestAccessControl.UseRoles({
    resource: "DeliveryAssignment",
    action: "read",
    possession: "any",
  })
  async findAssignments(
    @graphql.Parent() parent: Driver,
    @graphql.Args() args: DeliveryAssignmentFindManyArgs
  ): Promise<DeliveryAssignment[]> {
    const results = await this.service.findAssignments(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [AdminAuditLog], { name: "audits" })
  @nestAccessControl.UseRoles({
    resource: "AdminAuditLog",
    action: "read",
    possession: "any",
  })
  async findAudits(
    @graphql.Parent() parent: Driver,
    @graphql.Args() args: AdminAuditLogFindManyArgs
  ): Promise<AdminAuditLog[]> {
    const results = await this.service.findAudits(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DriverDistrictPreference], { name: "districts" })
  @nestAccessControl.UseRoles({
    resource: "DriverDistrictPreference",
    action: "read",
    possession: "any",
  })
  async findDistricts(
    @graphql.Parent() parent: Driver,
    @graphql.Args() args: DriverDistrictPreferenceFindManyArgs
  ): Promise<DriverDistrictPreference[]> {
    const results = await this.service.findDistricts(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [NotificationEvent], { name: "notifications" })
  @nestAccessControl.UseRoles({
    resource: "NotificationEvent",
    action: "read",
    possession: "any",
  })
  async findNotifications(
    @graphql.Parent() parent: Driver,
    @graphql.Args() args: NotificationEventFindManyArgs
  ): Promise<NotificationEvent[]> {
    const results = await this.service.findNotifications(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DriverPayout], { name: "payouts" })
  @nestAccessControl.UseRoles({
    resource: "DriverPayout",
    action: "read",
    possession: "any",
  })
  async findPayouts(
    @graphql.Parent() parent: Driver,
    @graphql.Args() args: DriverPayoutFindManyArgs
  ): Promise<DriverPayout[]> {
    const results = await this.service.findPayouts(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [DeliveryRating], { name: "ratingsReceived" })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRating",
    action: "read",
    possession: "any",
  })
  async findRatingsReceived(
    @graphql.Parent() parent: Driver,
    @graphql.Args() args: DeliveryRatingFindManyArgs
  ): Promise<DeliveryRating[]> {
    const results = await this.service.findRatingsReceived(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DriverAlertPreference, {
    nullable: true,
    name: "alerts",
  })
  @nestAccessControl.UseRoles({
    resource: "DriverAlertPreference",
    action: "read",
    possession: "any",
  })
  async getAlerts(
    @graphql.Parent() parent: Driver
  ): Promise<DriverAlertPreference | null> {
    const result = await this.service.getAlerts(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "approvedBy",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getApprovedBy(@graphql.Parent() parent: Driver): Promise<User | null> {
    const result = await this.service.getApprovedBy(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DriverLocation, {
    nullable: true,
    name: "location",
  })
  @nestAccessControl.UseRoles({
    resource: "DriverLocation",
    action: "read",
    possession: "any",
  })
  async getLocation(
    @graphql.Parent() parent: Driver
  ): Promise<DriverLocation | null> {
    const result = await this.service.getLocation(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DriverPreference, {
    nullable: true,
    name: "preferences",
  })
  @nestAccessControl.UseRoles({
    resource: "DriverPreference",
    action: "read",
    possession: "any",
  })
  async getPreferences(
    @graphql.Parent() parent: Driver
  ): Promise<DriverPreference | null> {
    const result = await this.service.getPreferences(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getUser(@graphql.Parent() parent: Driver): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
