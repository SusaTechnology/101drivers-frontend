/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { EmailVerificationTokenService } from "../emailVerificationToken.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EmailVerificationTokenCreateInput } from "./EmailVerificationTokenCreateInput";
import { EmailVerificationToken } from "./EmailVerificationToken";
import { EmailVerificationTokenFindManyArgs } from "./EmailVerificationTokenFindManyArgs";
import { EmailVerificationTokenWhereUniqueInput } from "./EmailVerificationTokenWhereUniqueInput";
import { EmailVerificationTokenUpdateInput } from "./EmailVerificationTokenUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class EmailVerificationTokenControllerBase {
  constructor(
    protected readonly service: EmailVerificationTokenService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: EmailVerificationToken })
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createEmailVerificationToken(
    @common.Body() data: EmailVerificationTokenCreateInput
  ): Promise<EmailVerificationToken> {
    return await this.service.createEmailVerificationToken({
      data: data,
      select: {
        createdAt: true,
        email: true,
        expiresAt: true,
        id: true,
        purpose: true,
        tokenHash: true,
        verifiedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [EmailVerificationToken] })
  @ApiNestedQuery(EmailVerificationTokenFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async emailVerificationTokens(
    @common.Req() request: Request
  ): Promise<EmailVerificationToken[]> {
    const args = plainToClass(
      EmailVerificationTokenFindManyArgs,
      request.query
    );
    return this.service.emailVerificationTokens({
      ...args,
      select: {
        createdAt: true,
        email: true,
        expiresAt: true,
        id: true,
        purpose: true,
        tokenHash: true,
        verifiedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: EmailVerificationToken })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async emailVerificationToken(
    @common.Param() params: EmailVerificationTokenWhereUniqueInput
  ): Promise<EmailVerificationToken | null> {
    const result = await this.service.emailVerificationToken({
      where: params,
      select: {
        createdAt: true,
        email: true,
        expiresAt: true,
        id: true,
        purpose: true,
        tokenHash: true,
        verifiedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: EmailVerificationToken })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateEmailVerificationToken(
    @common.Param() params: EmailVerificationTokenWhereUniqueInput,
    @common.Body() data: EmailVerificationTokenUpdateInput
  ): Promise<EmailVerificationToken | null> {
    try {
      return await this.service.updateEmailVerificationToken({
        where: params,
        data: data,
        select: {
          createdAt: true,
          email: true,
          expiresAt: true,
          id: true,
          purpose: true,
          tokenHash: true,
          verifiedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: EmailVerificationToken })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteEmailVerificationToken(
    @common.Param() params: EmailVerificationTokenWhereUniqueInput
  ): Promise<EmailVerificationToken | null> {
    try {
      return await this.service.deleteEmailVerificationToken({
        where: params,
        select: {
          createdAt: true,
          email: true,
          expiresAt: true,
          id: true,
          purpose: true,
          tokenHash: true,
          verifiedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
