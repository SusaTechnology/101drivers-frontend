/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { EmailVerificationToken } from "./EmailVerificationToken";
import { EmailVerificationTokenCountArgs } from "./EmailVerificationTokenCountArgs";
import { EmailVerificationTokenFindManyArgs } from "./EmailVerificationTokenFindManyArgs";
import { EmailVerificationTokenFindUniqueArgs } from "./EmailVerificationTokenFindUniqueArgs";
import { CreateEmailVerificationTokenArgs } from "./CreateEmailVerificationTokenArgs";
import { UpdateEmailVerificationTokenArgs } from "./UpdateEmailVerificationTokenArgs";
import { DeleteEmailVerificationTokenArgs } from "./DeleteEmailVerificationTokenArgs";
import { EmailVerificationTokenService } from "../emailVerificationToken.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => EmailVerificationToken)
export class EmailVerificationTokenResolverBase {
  constructor(
    protected readonly service: EmailVerificationTokenService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "read",
    possession: "any",
  })
  async _emailVerificationTokensMeta(
    @graphql.Args() args: EmailVerificationTokenCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [EmailVerificationToken])
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "read",
    possession: "any",
  })
  async emailVerificationTokens(
    @graphql.Args() args: EmailVerificationTokenFindManyArgs
  ): Promise<EmailVerificationToken[]> {
    return this.service.emailVerificationTokens(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => EmailVerificationToken, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "read",
    possession: "own",
  })
  async emailVerificationToken(
    @graphql.Args() args: EmailVerificationTokenFindUniqueArgs
  ): Promise<EmailVerificationToken | null> {
    const result = await this.service.emailVerificationToken(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => EmailVerificationToken)
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "create",
    possession: "any",
  })
  async createEmailVerificationToken(
    @graphql.Args() args: CreateEmailVerificationTokenArgs
  ): Promise<EmailVerificationToken> {
    return await this.service.createEmailVerificationToken({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => EmailVerificationToken)
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "update",
    possession: "any",
  })
  async updateEmailVerificationToken(
    @graphql.Args() args: UpdateEmailVerificationTokenArgs
  ): Promise<EmailVerificationToken | null> {
    try {
      return await this.service.updateEmailVerificationToken({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => EmailVerificationToken)
  @nestAccessControl.UseRoles({
    resource: "EmailVerificationToken",
    action: "delete",
    possession: "any",
  })
  async deleteEmailVerificationToken(
    @graphql.Args() args: DeleteEmailVerificationTokenArgs
  ): Promise<EmailVerificationToken | null> {
    try {
      return await this.service.deleteEmailVerificationToken(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
