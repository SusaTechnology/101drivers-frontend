/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { PricingCategoryRule } from "./PricingCategoryRule";
import { PricingCategoryRuleCountArgs } from "./PricingCategoryRuleCountArgs";
import { PricingCategoryRuleFindManyArgs } from "./PricingCategoryRuleFindManyArgs";
import { PricingCategoryRuleFindUniqueArgs } from "./PricingCategoryRuleFindUniqueArgs";
import { CreatePricingCategoryRuleArgs } from "./CreatePricingCategoryRuleArgs";
import { UpdatePricingCategoryRuleArgs } from "./UpdatePricingCategoryRuleArgs";
import { DeletePricingCategoryRuleArgs } from "./DeletePricingCategoryRuleArgs";
import { PricingConfig } from "../../pricingConfig/base/PricingConfig";
import { PricingCategoryRuleService } from "../pricingCategoryRule.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PricingCategoryRule)
export class PricingCategoryRuleResolverBase {
  constructor(
    protected readonly service: PricingCategoryRuleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PricingCategoryRule",
    action: "read",
    possession: "any",
  })
  async _pricingCategoryRulesMeta(
    @graphql.Args() args: PricingCategoryRuleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PricingCategoryRule])
  @nestAccessControl.UseRoles({
    resource: "PricingCategoryRule",
    action: "read",
    possession: "any",
  })
  async pricingCategoryRules(
    @graphql.Args() args: PricingCategoryRuleFindManyArgs
  ): Promise<PricingCategoryRule[]> {
    return this.service.pricingCategoryRules(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PricingCategoryRule, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PricingCategoryRule",
    action: "read",
    possession: "own",
  })
  async pricingCategoryRule(
    @graphql.Args() args: PricingCategoryRuleFindUniqueArgs
  ): Promise<PricingCategoryRule | null> {
    const result = await this.service.pricingCategoryRule(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => PricingCategoryRule)
  @nestAccessControl.UseRoles({
    resource: "PricingCategoryRule",
    action: "create",
    possession: "any",
  })
  async createPricingCategoryRule(
    @graphql.Args() args: CreatePricingCategoryRuleArgs
  ): Promise<PricingCategoryRule> {
    return await this.service.createPricingCategoryRule({
      ...args,
      data: {
        ...args.data,

        pricingConfig: {
          connect: args.data.pricingConfig,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => PricingCategoryRule)
  @nestAccessControl.UseRoles({
    resource: "PricingCategoryRule",
    action: "update",
    possession: "any",
  })
  async updatePricingCategoryRule(
    @graphql.Args() args: UpdatePricingCategoryRuleArgs
  ): Promise<PricingCategoryRule | null> {
    try {
      return await this.service.updatePricingCategoryRule({
        ...args,
        data: {
          ...args.data,

          pricingConfig: {
            connect: args.data.pricingConfig,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => PricingCategoryRule)
  @nestAccessControl.UseRoles({
    resource: "PricingCategoryRule",
    action: "delete",
    possession: "any",
  })
  async deletePricingCategoryRule(
    @graphql.Args() args: DeletePricingCategoryRuleArgs
  ): Promise<PricingCategoryRule | null> {
    try {
      return await this.service.deletePricingCategoryRule(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => PricingConfig, {
    nullable: true,
    name: "pricingConfig",
  })
  @nestAccessControl.UseRoles({
    resource: "PricingConfig",
    action: "read",
    possession: "any",
  })
  async getPricingConfig(
    @graphql.Parent() parent: PricingCategoryRule
  ): Promise<PricingConfig | null> {
    const result = await this.service.getPricingConfig(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
