/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { EvidenceExport } from "./EvidenceExport";
import { EvidenceExportCountArgs } from "./EvidenceExportCountArgs";
import { EvidenceExportFindManyArgs } from "./EvidenceExportFindManyArgs";
import { EvidenceExportFindUniqueArgs } from "./EvidenceExportFindUniqueArgs";
import { CreateEvidenceExportArgs } from "./CreateEvidenceExportArgs";
import { UpdateEvidenceExportArgs } from "./UpdateEvidenceExportArgs";
import { DeleteEvidenceExportArgs } from "./DeleteEvidenceExportArgs";
import { User } from "../../user/base/User";
import { DeliveryRequest } from "../../deliveryRequest/base/DeliveryRequest";
import { EvidenceExportService } from "../evidenceExport.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => EvidenceExport)
export class EvidenceExportResolverBase {
  constructor(
    protected readonly service: EvidenceExportService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "EvidenceExport",
    action: "read",
    possession: "any",
  })
  async _evidenceExportsMeta(
    @graphql.Args() args: EvidenceExportCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [EvidenceExport])
  @nestAccessControl.UseRoles({
    resource: "EvidenceExport",
    action: "read",
    possession: "any",
  })
  async evidenceExports(
    @graphql.Args() args: EvidenceExportFindManyArgs
  ): Promise<EvidenceExport[]> {
    return this.service.evidenceExports(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => EvidenceExport, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EvidenceExport",
    action: "read",
    possession: "own",
  })
  async evidenceExport(
    @graphql.Args() args: EvidenceExportFindUniqueArgs
  ): Promise<EvidenceExport | null> {
    const result = await this.service.evidenceExport(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => EvidenceExport)
  @nestAccessControl.UseRoles({
    resource: "EvidenceExport",
    action: "create",
    possession: "any",
  })
  async createEvidenceExport(
    @graphql.Args() args: CreateEvidenceExportArgs
  ): Promise<EvidenceExport> {
    return await this.service.createEvidenceExport({
      ...args,
      data: {
        ...args.data,

        createdBy: args.data.createdBy
          ? {
              connect: args.data.createdBy,
            }
          : undefined,

        delivery: {
          connect: args.data.delivery,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => EvidenceExport)
  @nestAccessControl.UseRoles({
    resource: "EvidenceExport",
    action: "update",
    possession: "any",
  })
  async updateEvidenceExport(
    @graphql.Args() args: UpdateEvidenceExportArgs
  ): Promise<EvidenceExport | null> {
    try {
      return await this.service.updateEvidenceExport({
        ...args,
        data: {
          ...args.data,

          createdBy: args.data.createdBy
            ? {
                connect: args.data.createdBy,
              }
            : undefined,

          delivery: {
            connect: args.data.delivery,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => EvidenceExport)
  @nestAccessControl.UseRoles({
    resource: "EvidenceExport",
    action: "delete",
    possession: "any",
  })
  async deleteEvidenceExport(
    @graphql.Args() args: DeleteEvidenceExportArgs
  ): Promise<EvidenceExport | null> {
    try {
      return await this.service.deleteEvidenceExport(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "createdBy",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getCreatedBy(
    @graphql.Parent() parent: EvidenceExport
  ): Promise<User | null> {
    const result = await this.service.getCreatedBy(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => DeliveryRequest, {
    nullable: true,
    name: "delivery",
  })
  @nestAccessControl.UseRoles({
    resource: "DeliveryRequest",
    action: "read",
    possession: "any",
  })
  async getDelivery(
    @graphql.Parent() parent: EvidenceExport
  ): Promise<DeliveryRequest | null> {
    const result = await this.service.getDelivery(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
