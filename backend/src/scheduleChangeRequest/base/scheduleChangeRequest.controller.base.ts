/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ScheduleChangeRequestService } from "../scheduleChangeRequest.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ScheduleChangeRequestCreateInput } from "./ScheduleChangeRequestCreateInput";
import { ScheduleChangeRequest } from "./ScheduleChangeRequest";
import { ScheduleChangeRequestFindManyArgs } from "./ScheduleChangeRequestFindManyArgs";
import { ScheduleChangeRequestWhereUniqueInput } from "./ScheduleChangeRequestWhereUniqueInput";
import { ScheduleChangeRequestUpdateInput } from "./ScheduleChangeRequestUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ScheduleChangeRequestControllerBase {
  constructor(
    protected readonly service: ScheduleChangeRequestService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ScheduleChangeRequest })
  @nestAccessControl.UseRoles({
    resource: "ScheduleChangeRequest",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createScheduleChangeRequest(
    @common.Body() data: ScheduleChangeRequestCreateInput
  ): Promise<ScheduleChangeRequest> {
    return await this.service.createScheduleChangeRequest({
      data: {
        ...data,

        decidedBy: data.decidedBy
          ? {
              connect: data.decidedBy,
            }
          : undefined,

        delivery: {
          connect: data.delivery,
        },

        requestedBy: data.requestedBy
          ? {
              connect: data.requestedBy,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        decidedAt: true,

        decidedBy: {
          select: {
            id: true,
          },
        },

        decisionNote: true,

        delivery: {
          select: {
            id: true,
          },
        },

        id: true,
        proposedDropoffWindowEnd: true,
        proposedDropoffWindowStart: true,
        proposedPickupWindowEnd: true,
        proposedPickupWindowStart: true,
        reason: true,

        requestedBy: {
          select: {
            id: true,
          },
        },

        requestedByRole: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ScheduleChangeRequest] })
  @ApiNestedQuery(ScheduleChangeRequestFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ScheduleChangeRequest",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async scheduleChangeRequests(
    @common.Req() request: Request
  ): Promise<ScheduleChangeRequest[]> {
    const args = plainToClass(ScheduleChangeRequestFindManyArgs, request.query);
    return this.service.scheduleChangeRequests({
      ...args,
      select: {
        createdAt: true,
        decidedAt: true,

        decidedBy: {
          select: {
            id: true,
          },
        },

        decisionNote: true,

        delivery: {
          select: {
            id: true,
          },
        },

        id: true,
        proposedDropoffWindowEnd: true,
        proposedDropoffWindowStart: true,
        proposedPickupWindowEnd: true,
        proposedPickupWindowStart: true,
        reason: true,

        requestedBy: {
          select: {
            id: true,
          },
        },

        requestedByRole: true,
        status: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ScheduleChangeRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ScheduleChangeRequest",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async scheduleChangeRequest(
    @common.Param() params: ScheduleChangeRequestWhereUniqueInput
  ): Promise<ScheduleChangeRequest | null> {
    const result = await this.service.scheduleChangeRequest({
      where: params,
      select: {
        createdAt: true,
        decidedAt: true,

        decidedBy: {
          select: {
            id: true,
          },
        },

        decisionNote: true,

        delivery: {
          select: {
            id: true,
          },
        },

        id: true,
        proposedDropoffWindowEnd: true,
        proposedDropoffWindowStart: true,
        proposedPickupWindowEnd: true,
        proposedPickupWindowStart: true,
        reason: true,

        requestedBy: {
          select: {
            id: true,
          },
        },

        requestedByRole: true,
        status: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ScheduleChangeRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ScheduleChangeRequest",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateScheduleChangeRequest(
    @common.Param() params: ScheduleChangeRequestWhereUniqueInput,
    @common.Body() data: ScheduleChangeRequestUpdateInput
  ): Promise<ScheduleChangeRequest | null> {
    try {
      return await this.service.updateScheduleChangeRequest({
        where: params,
        data: {
          ...data,

          decidedBy: data.decidedBy
            ? {
                connect: data.decidedBy,
              }
            : undefined,

          delivery: {
            connect: data.delivery,
          },

          requestedBy: data.requestedBy
            ? {
                connect: data.requestedBy,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          decidedAt: true,

          decidedBy: {
            select: {
              id: true,
            },
          },

          decisionNote: true,

          delivery: {
            select: {
              id: true,
            },
          },

          id: true,
          proposedDropoffWindowEnd: true,
          proposedDropoffWindowStart: true,
          proposedPickupWindowEnd: true,
          proposedPickupWindowStart: true,
          reason: true,

          requestedBy: {
            select: {
              id: true,
            },
          },

          requestedByRole: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ScheduleChangeRequest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ScheduleChangeRequest",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteScheduleChangeRequest(
    @common.Param() params: ScheduleChangeRequestWhereUniqueInput
  ): Promise<ScheduleChangeRequest | null> {
    try {
      return await this.service.deleteScheduleChangeRequest({
        where: params,
        select: {
          createdAt: true,
          decidedAt: true,

          decidedBy: {
            select: {
              id: true,
            },
          },

          decisionNote: true,

          delivery: {
            select: {
              id: true,
            },
          },

          id: true,
          proposedDropoffWindowEnd: true,
          proposedDropoffWindowStart: true,
          proposedPickupWindowEnd: true,
          proposedPickupWindowStart: true,
          reason: true,

          requestedBy: {
            select: {
              id: true,
            },
          },

          requestedByRole: true,
          status: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
