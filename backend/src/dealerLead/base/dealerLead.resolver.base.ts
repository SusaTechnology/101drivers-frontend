/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DealerLead } from "./DealerLead";
import { DealerLeadCountArgs } from "./DealerLeadCountArgs";
import { DealerLeadFindManyArgs } from "./DealerLeadFindManyArgs";
import { DealerLeadFindUniqueArgs } from "./DealerLeadFindUniqueArgs";
import { CreateDealerLeadArgs } from "./CreateDealerLeadArgs";
import { UpdateDealerLeadArgs } from "./UpdateDealerLeadArgs";
import { DeleteDealerLeadArgs } from "./DeleteDealerLeadArgs";
import { DealerLeadService } from "../dealerLead.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DealerLead)
export class DealerLeadResolverBase {
  constructor(
    protected readonly service: DealerLeadService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "read",
    possession: "any",
  })
  async _dealerLeadsMeta(
    @graphql.Args() args: DealerLeadCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DealerLead])
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "read",
    possession: "any",
  })
  async dealerLeads(
    @graphql.Args() args: DealerLeadFindManyArgs
  ): Promise<DealerLead[]> {
    return this.service.dealerLeads(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DealerLead, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "read",
    possession: "own",
  })
  async dealerLead(
    @graphql.Args() args: DealerLeadFindUniqueArgs
  ): Promise<DealerLead | null> {
    const result = await this.service.dealerLead(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DealerLead)
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "create",
    possession: "any",
  })
  async createDealerLead(
    @graphql.Args() args: CreateDealerLeadArgs
  ): Promise<DealerLead> {
    return await this.service.createDealerLead({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DealerLead)
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "update",
    possession: "any",
  })
  async updateDealerLead(
    @graphql.Args() args: UpdateDealerLeadArgs
  ): Promise<DealerLead | null> {
    try {
      return await this.service.updateDealerLead({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DealerLead)
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "delete",
    possession: "any",
  })
  async deleteDealerLead(
    @graphql.Args() args: DeleteDealerLeadArgs
  ): Promise<DealerLead | null> {
    try {
      return await this.service.deleteDealerLead(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
