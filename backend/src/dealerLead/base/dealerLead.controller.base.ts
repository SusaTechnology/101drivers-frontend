/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DealerLeadService } from "../dealerLead.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DealerLeadCreateInput } from "./DealerLeadCreateInput";
import { DealerLead } from "./DealerLead";
import { DealerLeadFindManyArgs } from "./DealerLeadFindManyArgs";
import { DealerLeadWhereUniqueInput } from "./DealerLeadWhereUniqueInput";
import { DealerLeadUpdateInput } from "./DealerLeadUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DealerLeadControllerBase {
  constructor(
    protected readonly service: DealerLeadService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DealerLead })
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDealerLead(
    @common.Body() data: DealerLeadCreateInput
  ): Promise<DealerLead> {
    return await this.service.createDealerLead({
      data: data,
      select: {
        businessName: true,
        createdAt: true,
        email: true,
        id: true,
        message: true,
        phone: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DealerLead] })
  @ApiNestedQuery(DealerLeadFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dealerLeads(@common.Req() request: Request): Promise<DealerLead[]> {
    const args = plainToClass(DealerLeadFindManyArgs, request.query);
    return this.service.dealerLeads({
      ...args,
      select: {
        businessName: true,
        createdAt: true,
        email: true,
        id: true,
        message: true,
        phone: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DealerLead })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dealerLead(
    @common.Param() params: DealerLeadWhereUniqueInput
  ): Promise<DealerLead | null> {
    const result = await this.service.dealerLead({
      where: params,
      select: {
        businessName: true,
        createdAt: true,
        email: true,
        id: true,
        message: true,
        phone: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DealerLead })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDealerLead(
    @common.Param() params: DealerLeadWhereUniqueInput,
    @common.Body() data: DealerLeadUpdateInput
  ): Promise<DealerLead | null> {
    try {
      return await this.service.updateDealerLead({
        where: params,
        data: data,
        select: {
          businessName: true,
          createdAt: true,
          email: true,
          id: true,
          message: true,
          phone: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DealerLead })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DealerLead",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDealerLead(
    @common.Param() params: DealerLeadWhereUniqueInput
  ): Promise<DealerLead | null> {
    try {
      return await this.service.deleteDealerLead({
        where: params,
        select: {
          businessName: true,
          createdAt: true,
          email: true,
          id: true,
          message: true,
          phone: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
